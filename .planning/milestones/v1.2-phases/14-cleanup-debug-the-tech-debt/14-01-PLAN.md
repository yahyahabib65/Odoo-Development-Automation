---
phase: 14-cleanup-debug-the-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/odoo_gen_utils/auto_fix.py
  - src/odoo_gen_utils/cli.py
  - tests/test_auto_fix.py
autonomous: true
requirements:
  - TDEBT-01
  - TDEBT-02

must_haves:
  truths:
    - "validate --auto-fix triggers Docker fix dispatch when Docker validation fails"
    - "fix_missing_mail_thread executes at runtime when Docker error contains mail.thread pattern"
    - "fix_unused_imports executes at runtime during pylint fix loop when unused-import violations detected"
  artifacts:
    - path: "src/odoo_gen_utils/auto_fix.py"
      provides: "run_docker_fix_loop() function"
      contains: "def run_docker_fix_loop"
    - path: "src/odoo_gen_utils/cli.py"
      provides: "Docker fix dispatch after Docker validation"
      contains: "run_docker_fix_loop"
    - path: "tests/test_auto_fix.py"
      provides: "Integration tests for Docker fix dispatch"
      contains: "test_run_docker_fix_loop"
  key_links:
    - from: "src/odoo_gen_utils/cli.py"
      to: "src/odoo_gen_utils/auto_fix.py"
      via: "import run_docker_fix_loop"
      pattern: "from odoo_gen_utils\\.auto_fix import.*run_docker_fix_loop"
    - from: "src/odoo_gen_utils/auto_fix.py:run_docker_fix_loop"
      to: "src/odoo_gen_utils/auto_fix.py:identify_docker_fix"
      via: "function call"
      pattern: "identify_docker_fix\\("
    - from: "src/odoo_gen_utils/auto_fix.py:run_docker_fix_loop"
      to: "src/odoo_gen_utils/auto_fix.py:fix_missing_mail_thread"
      via: "dispatch dict"
      pattern: "fix_missing_mail_thread"
---

<objective>
Wire orphaned auto-fix functions into CLI runtime

Purpose: fix_missing_mail_thread() and fix_unused_imports() are implemented and tested (15 tests pass) but have no runtime caller. The validate CLI command's --auto-fix flag only invokes run_pylint_fix_loop. This plan creates a run_docker_fix_loop() dispatcher and wires it into the CLI so both fix functions execute at runtime.

Output: run_docker_fix_loop() in auto_fix.py, updated CLI validate command, integration tests
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cleanup-debug-the-tech-debt/14-CONTEXT.md
@.planning/v1.2-MILESTONE-AUDIT.md
@src/odoo_gen_utils/auto_fix.py
@src/odoo_gen_utils/cli.py
@tests/test_auto_fix.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create run_docker_fix_loop and wire fix_unused_imports into pylint loop</name>
  <files>src/odoo_gen_utils/auto_fix.py, tests/test_auto_fix.py</files>
  <behavior>
    - Test 1: run_docker_fix_loop with error text containing mail.thread keywords calls fix_missing_mail_thread and returns True
    - Test 2: run_docker_fix_loop with error text containing unused import keywords calls fix_unused_imports and returns True
    - Test 3: run_docker_fix_loop with unrecognized error text returns False (no fix applied)
    - Test 4: run_docker_fix_loop with empty error text returns False
    - Test 5: run_pylint_fix_loop calls fix_unused_imports when pylint reports unused-import (W0611) violations
  </behavior>
  <action>
    1. Create run_docker_fix_loop(module_path: Path, error_output: str) -> bool in auto_fix.py following the immutable read-transform-write pattern established by run_pylint_fix_loop (line 297-340).
    2. The function calls identify_docker_fix(error_output) (already implemented at line 342) to detect the error pattern name.
    3. Dispatch to fix functions via a dict: {"missing_mail_thread": fix_missing_mail_thread, ...}. Call the matched fix function with module_path. Return True if a fix was applied, False otherwise.
    4. Also extend run_pylint_fix_loop to call fix_unused_imports when pylint violations include W0611 (unused-import). This wires fix_unused_imports into the pylint path per user decision: "fix_unused_imports should also run during pylint fix loop when unused-import pylint violations are detected".
    5. Add proper logging: log which pattern was detected, which fix function was dispatched, and whether the fix succeeded.
    6. Write tests FIRST (RED), then implement (GREEN). Add tests to existing tests/test_auto_fix.py.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation && uv run pytest tests/test_auto_fix.py -x -v -k "docker_fix_loop or pylint_unused" 2>&1 | tail -20</automated>
  </verify>
  <done>run_docker_fix_loop dispatches to fix_missing_mail_thread and fix_unused_imports based on identify_docker_fix output. run_pylint_fix_loop invokes fix_unused_imports for W0611 violations. All new tests pass alongside existing 15 tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire run_docker_fix_loop into CLI validate command</name>
  <files>src/odoo_gen_utils/cli.py, tests/test_auto_fix.py</files>
  <action>
    1. In cli.py line 12 (import statement), add run_docker_fix_loop to the existing import: `from odoo_gen_utils.auto_fix import format_escalation, run_pylint_fix_loop, run_docker_fix_loop`
    2. In cli.py around line 413 (the `if auto_fix:` block), after Docker validation fails:
       - Call run_docker_fix_loop(module_path, docker_error_output)
       - If it returns True, re-run Docker validation (single retry, not a loop -- per existing pattern)
       - Log the fix attempt and result
    3. The --auto-fix flag triggers BOTH pylint fix loop AND Docker fix dispatch (per user decision: "not a separate flag").
    4. Add one integration test in test_auto_fix.py that verifies the import path works: `from odoo_gen_utils.auto_fix import run_docker_fix_loop` succeeds and the function is callable.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation && uv run python -c "from odoo_gen_utils.auto_fix import run_docker_fix_loop; print('import OK')" && uv run pytest tests/test_auto_fix.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>CLI validate --auto-fix imports and calls run_docker_fix_loop after Docker validation failures. All existing tests (15+) plus new tests pass. No orphaned exports remain -- identify_docker_fix, fix_missing_mail_thread, and fix_unused_imports all have runtime callers.</done>
</task>

</tasks>

<verification>
1. All tests pass: `uv run pytest tests/test_auto_fix.py -x -v` (existing 15 + new tests)
2. Full test suite passes: `uv run pytest tests/ -x --timeout=60`
3. Import chain verified: `uv run python -c "from odoo_gen_utils.auto_fix import run_docker_fix_loop; from odoo_gen_utils.cli import validate"`
4. No orphaned exports: grep for fix_missing_mail_thread, fix_unused_imports, identify_docker_fix in cli.py or auto_fix.py shows callers beyond tests
</verification>

<success_criteria>
- run_docker_fix_loop() exists in auto_fix.py and dispatches to fix functions via identify_docker_fix()
- cli.py validate --auto-fix calls run_docker_fix_loop after Docker validation failures
- fix_unused_imports also runs during pylint fix loop for W0611 violations
- All 3 previously-orphaned functions (fix_missing_mail_thread, fix_unused_imports, identify_docker_fix) have runtime callers
- All tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/14-cleanup-debug-the-tech-debt/14-01-SUMMARY.md`
</output>
