---
phase: 01-gsd-extension
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/new.md
  - commands/help.md
  - commands/config.md
  - commands/status.md
  - commands/resume.md
  - commands/phases.md
  - commands/validate.md
  - commands/search.md
  - commands/research.md
  - commands/plan.md
  - commands/extend.md
  - commands/history.md
autonomous: true
requirements:
  - EXT-02

must_haves:
  truths:
    - "/odoo-gen:new command file exists with argument-hint, agent reference to odoo-scaffold, and allowed-tools"
    - "/odoo-gen:help command file exists with description of all available commands"
    - "Wrapper commands (config, status, resume, phases) exist and reference their GSD equivalents with Odoo context"
    - "Stub commands (validate, search, research, plan, extend, history) exist with descriptions and inform user they are not yet available"
    - "All 12 command files have valid YAML frontmatter matching GSD command format"
  artifacts:
    - path: "commands/new.md"
      provides: "Entry point for /odoo-gen:new command"
      contains: "odoo-scaffold"
    - path: "commands/help.md"
      provides: "/odoo-gen:help command"
      contains: "odoo-gen"
    - path: "commands/config.md"
      provides: "/odoo-gen:config wrapper command"
      contains: "config"
    - path: "commands/status.md"
      provides: "/odoo-gen:status wrapper command"
      contains: "status"
    - path: "commands/resume.md"
      provides: "/odoo-gen:resume wrapper command"
      contains: "resume"
    - path: "commands/phases.md"
      provides: "/odoo-gen:phases wrapper command"
      contains: "phases"
    - path: "commands/validate.md"
      provides: "/odoo-gen:validate stub command"
      contains: "Phase 3"
    - path: "commands/search.md"
      provides: "/odoo-gen:search stub command"
      contains: "Phase 8"
    - path: "commands/research.md"
      provides: "/odoo-gen:research stub command"
      contains: "Phase 2"
    - path: "commands/plan.md"
      provides: "/odoo-gen:plan stub command"
      contains: "Phase 4"
    - path: "commands/extend.md"
      provides: "/odoo-gen:extend stub command"
      contains: "Phase 8"
    - path: "commands/history.md"
      provides: "/odoo-gen:history stub command"
      contains: "Phase 7"
  key_links:
    - from: "commands/new.md"
      to: "agents/odoo-scaffold.md"
      via: "agent frontmatter field"
      pattern: "agent: odoo-scaffold"
    - from: "commands/new.md"
      to: "workflows/scaffold.md"
      via: "@reference in execution_context (forward ref — target created in Plan 01-04, Wave 2)"
      pattern: "scaffold\\.md"
---

<objective>
Create all 12 `/odoo-gen:*` command .md files that Claude Code auto-discovers as slash commands.

Purpose: Register the full command surface area so users can invoke `/odoo-gen:new`, `/odoo-gen:help`, wrapper commands, and see informative descriptions for stub commands that will be implemented in later phases.

Output: 12 command .md files in `commands/` directory
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gsd-extension/01-RESEARCH.md
@.planning/phases/01-gsd-extension/01-CONTEXT.md

Reference GSD command format from: ~/.claude/commands/gsd/plan-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create core command files (new, help, config, status, resume, phases)</name>
  <files>
    commands/new.md
    commands/help.md
    commands/config.md
    commands/status.md
    commands/resume.md
    commands/phases.md
  </files>
  <action>
Create 6 command .md files following the exact GSD command format (YAML frontmatter with `name`, `description`, `argument-hint`, optional `agent`, `allowed-tools`).

**commands/new.md** — The primary command. Frontmatter:
```yaml
name: odoo-gen:new
description: Scaffold a new Odoo 17.0 module from a natural language description
argument-hint: "<module description>"
agent: odoo-scaffold
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - Task
```
Body: `<objective>` explaining the command scaffolds a complete Odoo 17.0 module. `<execution_context>` referencing `@~/.claude/odoo-gen/workflows/scaffold.md`. `<process>` section: "1. Parse module description from $ARGUMENTS. 2. Infer module spec (name, models, fields, views). 3. Show spec for user confirmation. 4. On approval, render all templates via odoo-gen-utils. 5. Report created files."

**commands/help.md** — Lists all available commands. Frontmatter:
```yaml
name: odoo-gen:help
description: Show all available odoo-gen commands and usage
```
No `agent` field — the command content IS the help text. Body lists all 12 commands in a table with name, description, and status (Active/Stub/Wrapper). Include usage examples for `/odoo-gen:new`.

**commands/config.md** — Wrapper around GSD config with Odoo context. Frontmatter:
```yaml
name: odoo-gen:config
description: View or edit Odoo-specific configuration (odoo_version, edition, output_dir)
argument-hint: "[key] [value]"
```
Body instructs to read `~/.claude/odoo-gen/defaults.json` for global defaults and `.planning/config.json` `odoo` section for project-specific overrides. If key+value provided, update the appropriate config. If only key, show current value. If no args, show all Odoo config.

**commands/status.md** — Wrapper around GSD status. Frontmatter:
```yaml
name: odoo-gen:status
description: Show current Odoo module generation status
```
Body: Read `.planning/STATE.md` and present current phase, plan, and any Odoo-specific state. Reference GSD's progress tracking.

**commands/resume.md** — Wrapper around GSD resume. Frontmatter:
```yaml
name: odoo-gen:resume
description: Resume an interrupted Odoo module generation session
```
Body: Read `.planning/STATE.md` session continuity section and resume from where generation stopped. Reference GSD's resume mechanism.

**commands/phases.md** — Show generation phases. Frontmatter:
```yaml
name: odoo-gen:phases
description: Show Odoo module generation phases and progress
```
Body: Read `.planning/ROADMAP.md` and present the 9-phase roadmap with current progress for each phase.
  </action>
  <verify>
Verify all 6 files exist in `commands/`. Each must have valid YAML frontmatter with at least `name` and `description`. `new.md` must have `agent: odoo-scaffold` and `allowed-tools` list. `help.md` must list all 12 commands. NOTE: `new.md` contains a forward reference to `@~/.claude/odoo-gen/workflows/scaffold.md` which is created in Plan 01-04 (Wave 2). This is intentional — the @reference is resolved at command invocation time, not at file creation time. During Wave 1 verification, confirm the reference path is correct but do not expect the target file to exist yet.
  </verify>
  <done>
6 core command files exist with correct GSD command format. /odoo-gen:new references the odoo-scaffold agent and scaffold workflow. /odoo-gen:help lists all 12 commands. Wrapper commands (config, status, resume, phases) reference their GSD equivalents with Odoo-specific context.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create stub command files (validate, search, research, plan, extend, history)</name>
  <files>
    commands/validate.md
    commands/search.md
    commands/research.md
    commands/plan.md
    commands/extend.md
    commands/history.md
  </files>
  <action>
Create 6 stub command .md files. Each stub has the same structure:

1. YAML frontmatter with `name` and `description` (no `agent` field since they are not yet active)
2. Body explains the command's purpose, what it will do when implemented, and which phase activates it
3. Instructs: "This command is not yet available. It will be implemented in Phase [N]. Run `/odoo-gen:help` to see currently available commands."

Specific stubs:

**commands/validate.md**:
```yaml
name: odoo-gen:validate
description: Run pylint-odoo and Docker-based validation on an Odoo module
argument-hint: "[module_path]"
```
"Activated in Phase 3 (Validation Infrastructure). Will run pylint-odoo on all Python/XML files and optionally spin up a Docker Odoo 17.0 instance to test module installation."

**commands/search.md**:
```yaml
name: odoo-gen:search
description: Semantically search GitHub/OCA for existing Odoo modules similar to your description
argument-hint: "<search query>"
```
"Activated in Phase 8 (Search & Fork-Extend). Will search OCA and GitHub repositories using semantic matching."

**commands/research.md**:
```yaml
name: odoo-gen:research
description: Research Odoo patterns and existing solutions for a module need
argument-hint: "<research topic>"
```
"Activated in Phase 2 (Knowledge Base). Will leverage the Odoo knowledge base to research patterns."

**commands/plan.md**:
```yaml
name: odoo-gen:plan
description: Plan module architecture before generation
argument-hint: "<module description>"
```
"Activated in Phase 4 (Input & Specification). Will create a detailed module architecture plan."

**commands/extend.md**:
```yaml
name: odoo-gen:extend
description: Fork and extend an existing Odoo module
argument-hint: "<module_name or github_url>"
```
"Activated in Phase 8 (Search & Fork-Extend). Will clone a module and generate delta code."

**commands/history.md**:
```yaml
name: odoo-gen:history
description: Show generation history and past modules
```
"Activated in Phase 7 (Human Review & Quality Loops). Will show past module generations."
  </action>
  <verify>
Verify all 6 stub files exist in `commands/`. Each must have valid YAML frontmatter with `name` and `description`. Each body must mention its activation phase number and suggest `/odoo-gen:help`.
  </verify>
  <done>
6 stub command files exist. Each has correct GSD command format, describes what it will do when active, references the specific phase that activates it, and directs users to /odoo-gen:help for currently available commands.
  </done>
</task>

</tasks>

<verification>
1. All 12 command .md files exist in `commands/` directory
2. Each file has valid YAML frontmatter with `name` and `description`
3. `new.md` has `agent: odoo-scaffold` and `allowed-tools` fields
4. `help.md` lists all 12 commands
5. Stub commands reference their activation phase
6. Command names follow `odoo-gen:*` namespace
</verification>

<success_criteria>
- All 12 `/odoo-gen:*` commands are registered as .md files
- /odoo-gen:new is the fully defined primary command with agent reference
- /odoo-gen:help provides a complete command reference
- Wrapper commands (config, status, resume, phases) add Odoo context to GSD equivalents
- Stub commands inform users about future availability with phase references
</success_criteria>

<output>
After completion, create `.planning/phases/01-gsd-extension/01-02-SUMMARY.md`
</output>
