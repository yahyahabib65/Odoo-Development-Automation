---
phase: 04-input-specification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/spec.md
  - commands/plan.md
  - agents/odoo-scaffold.md
autonomous: true
requirements:
  - INPT-01
  - INPT-02
  - INPT-03

must_haves:
  truths:
    - "/odoo-gen:plan command accepts a natural language module description and routes to the odoo-scaffold agent with spec workflow"
    - "Spec workflow defines 4 phases: NL parsing, tiered follow-up questions, structured spec generation, and approval presentation"
    - "Follow-up questions are tiered: 3-5 high-level Tier 1 questions always asked, 2-3 deeper Tier 2 questions triggered by complexity signals"
    - "Questions are Odoo-specific and informed by knowledge base (references to Odoo concepts like workflow states, inheritance, portal access)"
    - "Spec workflow produces a structured JSON spec compatible with render-module but extended with workflow_states, constraints, inherit, security_groups, menu_structure, demo_data_hints"
    - "Smart defaults are applied from keyword-to-type mapping: name->Char, amount->Float, partner->Many2one(res.partner), etc."
    - "odoo-scaffold agent references spec workflow and loads knowledge base for follow-up question generation"
  artifacts:
    - path: "workflows/spec.md"
      provides: "Complete specification workflow (parse, questions, spec, approve)"
      min_lines: 200
    - path: "commands/plan.md"
      provides: "Active /odoo-gen:plan command routing to odoo-scaffold with spec workflow"
      contains: "odoo-scaffold"
    - path: "agents/odoo-scaffold.md"
      provides: "Updated agent with spec workflow reference and dual-mode operation"
      contains: "spec.md"
  key_links:
    - from: "commands/plan.md"
      to: "agents/odoo-scaffold.md"
      via: "agent frontmatter field"
      pattern: "agent: odoo-scaffold"
    - from: "commands/plan.md"
      to: "workflows/spec.md"
      via: "@reference in execution_context"
      pattern: "spec\\.md"
    - from: "agents/odoo-scaffold.md"
      to: "workflows/spec.md"
      via: "@reference in role section"
      pattern: "spec\\.md"
    - from: "workflows/spec.md"
      to: "knowledge/MASTER.md"
      via: "@include reference for KB-informed questions"
      pattern: "knowledge"
---

<objective>
Create the specification workflow, activate the `/odoo-gen:plan` command, and update the odoo-scaffold agent to support both quick scaffold (via `/odoo-gen:new`) and full specification (via `/odoo-gen:plan`).

Purpose: Enable users to describe a module in natural language, receive targeted Odoo-specific follow-up questions, and get a structured JSON module specification -- the core input pipeline for code generation.

Output: `workflows/spec.md` (new), updated `commands/plan.md`, updated `agents/odoo-scaffold.md`
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-input-specification/04-RESEARCH.md
@.planning/phases/04-input-specification/04-CONTEXT.md

Reference existing patterns:
@workflows/scaffold.md
@commands/new.md
@agents/odoo-scaffold.md
@knowledge/MASTER.md
</context>

<interfaces>
<!-- Key interfaces the executor needs from existing codebase -->

From workflows/scaffold.md (Phase 1 Input Parsing pattern):
- Step-by-step workflow with ## Phase N headers
- Steps numbered within each phase
- Field inference table (Context Clue -> Odoo Field Type)
- Dependency inference table (Description mentions -> Add dependency)
- Defaults read from ~/.claude/odoo-gen/defaults.json

From commands/new.md (GSD command format):
- YAML frontmatter: name, description, argument-hint, agent, allowed-tools
- Body: objective, execution_context (@reference), process steps

From agents/odoo-scaffold.md (agent format):
- YAML frontmatter: name, description, tools, color
- role section with workflow phases, Odoo 17.0 specifics, KB references

From python/src/odoo_gen_utils/renderer.py (render_module spec format):
- Required: module_name
- Optional (with defaults): module_title, summary, author, website, license, category, odoo_version, application, depends, models[]
- Per model: name, description, fields[] (name, type, string, required, help, comodel_name)
- All field access uses .get() with defaults -- new fields are safe to add
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create the specification workflow</name>
  <files>workflows/spec.md</files>
  <action>
Create `workflows/spec.md` -- the complete specification pipeline workflow. Follow the same structure as `workflows/scaffold.md` (## Phase N headers with ### Steps subsections).

**Structure: 4 phases.**

**## Phase 1: Parse Natural Language Description**
Receive module description from `$ARGUMENTS`. Parse into a draft spec:
- Module technical name (snake_case from description, same rules as scaffold.md Phase 1 Step 2)
- Module title (human-readable)
- Summary (one-line from description)
- Category (infer from Odoo domain: Inventory, Sales, HR, etc.)
- Draft models extracted from nouns/noun phrases (dot-notation: `module_prefix.entity`)
- Draft fields for each model using the EXPANDED keyword-to-type mapping table:

| Keyword Pattern | Odoo Field Type | Default Config |
|----------------|-----------------|----------------|
| name, title | Char | required=True |
| description, notes, comment, body | Text | |
| email | Char | widget="email" |
| phone | Char | widget="phone" |
| url, website, link | Char | widget="url" |
| amount, price, cost, total | Float | digits=(16,2); use Monetary if currency field present |
| quantity, count, number, pages, age | Integer | |
| percentage, rate, ratio | Float | digits=(5,2) |
| date, deadline, start_date, end_date | Date | |
| datetime, timestamp, created, updated | Datetime | |
| active, is_*, has_* | Boolean | default=True for active |
| image, photo, avatar | Binary | |
| state, status | Selection | default="draft" |
| type, kind | Selection | |
| color | Integer | (Odoo color widget index) |
| priority | Selection | [("0","Normal"),("1","Low"),("2","High"),("3","Urgent")] |
| sequence | Integer | default=10 |
| partner, customer, vendor, supplier | Many2one("res.partner") | |
| user, responsible, assigned, salesperson | Many2one("res.users") | |
| company | Many2one("res.company") | index=True |
| currency | Many2one("res.currency") | |
| tag, category (plural context) | Many2many | |
| line, detail, item (plural context) | One2many | |

Always include per model: `name` (Char, required=True) and `active` (Boolean, default=True).

- Dependencies (same inference table as scaffold.md Phase 1 Step 6, plus `mail` if any model needs chatter)
- Read defaults from `~/.claude/odoo-gen/defaults.json` (odoo_version, license, author, website)

**## Phase 2: Tiered Follow-Up Questions**

### Tier 1 (Always Asked -- 3-5 questions)
Present these as a numbered list and wait for user answers:

1. **Models & Entities**: "Based on your description, I identified [models]. Are these the right data entities? Should any be added, removed, or renamed?"
2. **User Groups**: "Who will use this module? Typical Odoo pattern is User (read/write/create) and Manager (full access). Do you need different roles?"
3. **Module Integration**: "Should this module extend or integrate with existing Odoo apps? [list inferred dependencies]. Any others? (e.g., stock, sale, purchase, hr, account, website)"
4. **Workflow/Status**: "Should records have a workflow? Odoo supports state-based workflows (e.g., Draft -> Confirmed -> Done) with button transitions and group-based approval. Describe your workflow if needed."
5. (Only if description mentions customer/portal/website) **Portal Access**: "Should external users (customers/vendors) see this data through the Odoo portal?"

### Complexity Detection
After Tier 1 answers, scan for complexity triggers:

| Trigger Keywords | Complexity Signal | Tier 2 Question |
|-----------------|-------------------|-----------------|
| approval, validate, confirm | Workflow | "What are the specific states and transitions? Who can approve at each step?" |
| multi-company, branch, subsidiary | Multi-company | "Should records be company-specific? Shared across companies or isolated?" |
| extend, inherit, add field to, modify | Inheritance | "Which specific Odoo models should be extended? What fields/views should be added?" |
| portal, website, public, customer access | Portal | "What data should portal users see? Read-only or editable? Which fields visible?" |
| report, dashboard, KPI, analytics | Reporting | "What metrics/KPIs matter? Grouped by what dimensions? Charts or pivot tables?" |
| import, export, sync, API | Integration | "What external systems need data exchange? What format (CSV, XML, REST API)?" |
| schedule, cron, automated, recurring | Automation | "What should happen automatically? At what frequency? What triggers the action?" |

### Tier 2 (Conditional -- 0-3 questions)
Only ask Tier 2 questions for detected complexity signals. Maximum 3 additional questions. If no complexity detected, skip Tier 2 entirely.

After all answers collected, proceed to Phase 3.

**## Phase 3: Generate Structured Specification**

Build the complete spec.json from the parsed description + user answers. The JSON schema extends the `render-module` input format (backward-compatible):

```json
{
  "module_name": "snake_case_name",
  "module_title": "Human Readable Name",
  "summary": "One-line module description",
  "author": "from defaults.json",
  "website": "from defaults.json",
  "license": "from defaults.json (default LGPL-3)",
  "category": "Inferred Odoo category",
  "odoo_version": "from defaults.json (default 17.0)",
  "application": true,
  "depends": ["base", "...inferred and confirmed..."],
  "models": [
    {
      "name": "module.entity",
      "description": "Human description",
      "_inherit": null,
      "inherit_mixins": [],
      "fields": [
        {
          "name": "field_name",
          "type": "FieldType",
          "string": "Label",
          "required": false,
          "default": null,
          "help": "Help text",
          "comodel_name": null,
          "inverse_name": null,
          "selection": null,
          "compute": null,
          "widget": null,
          "tracking": false,
          "copy": true,
          "index": false,
          "groups": null
        }
      ],
      "constraints": [],
      "workflow_states": [],
      "sql_constraints": []
    }
  ],
  "views": [
    {
      "model": "module.entity",
      "types": ["form", "tree", "search"],
      "custom_fields_in_tree": [],
      "custom_search_filters": []
    }
  ],
  "security_groups": [
    {
      "name": "group_module_user",
      "label": "User",
      "permissions": {"read": true, "write": true, "create": true, "unlink": false}
    },
    {
      "name": "group_module_manager",
      "label": "Manager",
      "implied_ids": ["group_module_user"],
      "permissions": {"read": true, "write": true, "create": true, "unlink": true}
    }
  ],
  "menu_structure": {
    "root_menu": "Module Title",
    "sub_menus": ["Entity One", "Entity Two"]
  },
  "demo_data_hints": ["3 sample records per model"]
}
```

Rules for spec generation:
- All new fields (beyond Phase 1 render-module format) are OPTIONAL with null/empty defaults
- If user mentioned workflow states, populate `workflow_states` array with keys, labels, and `is_default` flag
- If user mentioned inheritance, set `_inherit` on the model
- If user mentioned chatter/messages/tracking, add `["mail.thread", "mail.activity.mixin"]` to `inherit_mixins` and `"mail"` to depends
- Always set `tracking: true` on `state` fields and `name` fields
- Apply smart defaults: if a field named "partner_id" was inferred, set `comodel_name: "res.partner"` automatically
- Populate `views` array with form+tree+search for each model
- Populate default security groups (User/Manager hierarchy)
- Menu structure: root menu = module title, sub-menus = one per model

**## Phase 4: Present Specification for Approval**

(Detailed in Plan 04-02. This phase renders the spec as markdown and presents for user approval.)

Summary placeholder: "Render spec.json as a human-readable markdown summary. Present to user for review. Wait for approval before proceeding."

**## Error Handling**
- If `$ARGUMENTS` is empty, prompt user: "Please describe the module you need."
- If user gives very vague description (< 5 words), infer a minimal spec and present it with: "I inferred the following from your brief description. Please confirm or provide more details."
- If defaults.json cannot be read, use hardcoded defaults (17.0, LGPL-3, empty author/website).

**## References**
- Agent: `agents/odoo-scaffold.md`
- Command: `commands/plan.md`
- Existing workflow: `workflows/scaffold.md` (pattern reference)
- Knowledge base: `~/.claude/odoo-gen/knowledge/` (loaded by agent for question generation)
  </action>
  <verify>
File `workflows/spec.md` exists with at least 200 lines. Contains all 4 phase headers (## Phase 1 through ## Phase 4). Contains the keyword-to-type mapping table. Contains Tier 1 questions (at least 4). Contains complexity trigger table. Contains the extended JSON spec schema. Contains references to knowledge base and defaults.json.

```bash
wc -l workflows/spec.md && grep -c "## Phase" workflows/spec.md && grep -c "Tier 1\|Tier 2" workflows/spec.md
```
  </verify>
  <done>
workflows/spec.md exists with complete 4-phase specification pipeline. Phase 1 parses NL to draft spec with expanded field inference. Phase 2 defines tiered follow-up questions with complexity detection. Phase 3 defines the extended JSON spec schema. Phase 4 is a placeholder for approval (detailed in Plan 04-02).
  </done>
</task>

<task type="auto">
  <name>Task 2: Activate /odoo-gen:plan command and update odoo-scaffold agent</name>
  <files>
    commands/plan.md
    agents/odoo-scaffold.md
  </files>
  <action>
**Update `commands/plan.md`** -- Replace the stub content with an active command:

```yaml
---
name: odoo-gen:plan
description: Plan and specify an Odoo 17.0 module from a natural language description
argument-hint: "<module description>"
agent: odoo-scaffold
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - Task
---
```

Body content:
- `<objective>`: "Create a detailed module specification from a natural language description. The system asks targeted Odoo-specific follow-up questions, produces a structured JSON specification, and presents it for user approval before any code generation begins. Unlike `/odoo-gen:new` (which scaffolds immediately), `/odoo-gen:plan` produces a thorough, reviewed specification that becomes the generation contract."
- `<execution_context>`: `@~/.claude/odoo-gen/workflows/spec.md`
- `<process>`:
  1. Parse module description from $ARGUMENTS
  2. Infer draft specification (module name, models, fields, dependencies)
  3. Ask tiered Odoo-specific follow-up questions (Tier 1: 3-5 always, Tier 2: 0-3 if complexity detected)
  4. Generate structured module specification (JSON)
  5. Render human-readable markdown summary
  6. Present for user approval (approve / request changes / edit directly)
  7. On approval, commit spec.json to git as generation contract

**Update `agents/odoo-scaffold.md`** -- Add dual-mode operation. Keep ALL existing content (the scaffold workflow, Odoo 17.0 specifics, OCA directory structure, model name conventions, security pattern, template rendering, knowledge base). Add a new section BEFORE the existing "## Workflow" section:

```markdown
## Mode Selection

This agent operates in TWO modes based on the invoking command:

### Quick Mode (via /odoo-gen:new)
Follows the scaffold workflow: parse description -> brief confirmation -> generate module immediately.
Reference: @~/.claude/odoo-gen/workflows/scaffold.md

### Specification Mode (via /odoo-gen:plan)
Follows the specification workflow: parse description -> tiered follow-up questions -> structured spec -> approval checkpoint -> spec.json committed.
Reference: @~/.claude/odoo-gen/workflows/spec.md

The mode is determined by which workflow the command's execution_context references. In specification mode, use the knowledge base extensively to generate Odoo-domain-specific follow-up questions.
```

Also add to the Knowledge Base section at the bottom of the agent:
```markdown
@~/.claude/odoo-gen/knowledge/inheritance.md
@~/.claude/odoo-gen/knowledge/actions.md
@~/.claude/odoo-gen/knowledge/wizards.md
@~/.claude/odoo-gen/knowledge/data.md
```

This gives the agent access to all 12 KB files (MASTER + models + views + security + manifest were already there, plus testing/actions/inheritance/data/i18n/controllers/wizards from Phase 2).
  </action>
  <verify>
Verify `commands/plan.md` has `agent: odoo-scaffold` in frontmatter, references `spec.md` workflow, and is no longer a stub (no "not yet available" text). Verify `agents/odoo-scaffold.md` has both mode descriptions and references both workflows. Verify KB references include at least 8 knowledge files.

```bash
grep -c "not yet available\|Not yet available" commands/plan.md && grep -c "spec.md\|scaffold.md" agents/odoo-scaffold.md && grep -c "@.*knowledge/" agents/odoo-scaffold.md
```

Expected: 0 (no stub text), 2+ (both workflows), 8+ (KB references).
  </verify>
  <done>
/odoo-gen:plan is an active command routing to the odoo-scaffold agent with spec workflow. The odoo-scaffold agent supports dual-mode operation: quick mode via /odoo-gen:new and specification mode via /odoo-gen:plan. Agent loads comprehensive knowledge base (8+ files) for domain-informed question generation.
  </done>
</task>

</tasks>

<verification>
1. `workflows/spec.md` exists with complete 4-phase specification pipeline (200+ lines)
2. `commands/plan.md` is active (not a stub) with correct frontmatter and spec.md reference
3. `agents/odoo-scaffold.md` references both scaffold.md and spec.md workflows
4. Spec workflow contains tiered question strategy with complexity triggers
5. Extended JSON spec schema is backward-compatible with existing render-module format
6. All smart default mappings are documented in the workflow
</verification>

<success_criteria>
- User can invoke `/odoo-gen:plan "description"` and the system accepts the natural language input (INPT-01)
- The specification workflow defines targeted Odoo-specific follow-up questions about models, fields, views, inheritance, and groups (INPT-02)
- The workflow produces a structured module specification with model names, field types, relationships, views, workflow states (INPT-03)
- The odoo-scaffold agent loads knowledge base files to inform domain-specific questions
- The spec JSON format extends (not breaks) the existing render-module input
</success_criteria>

<output>
After completion, create `.planning/phases/04-input-specification/04-01-SUMMARY.md`
</output>
