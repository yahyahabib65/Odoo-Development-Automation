---
phase: 04-input-specification
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - workflows/spec.md
autonomous: false
requirements:
  - INPT-04

must_haves:
  truths:
    - "Spec workflow Phase 4 renders spec.json as a human-readable markdown summary with sections: Module Overview, Models & Fields table, Relationships, Views, Security Groups, Workflow States"
    - "Markdown summary shows all inferred defaults explicitly -- nothing is silently assumed"
    - "User is presented three options: Approve (proceed), Request Changes (re-ask targeted questions), Edit Directly (user modifies summary text)"
    - "Approval step is a GSD checkpoint -- generation does not begin until user explicitly approves"
    - "On approval, spec.json is written to ./module_name/spec.json and committed to git as the generation contract"
    - "On 'Request Changes', system asks targeted follow-up questions about the specific sections the user flagged, then regenerates the spec"
  artifacts:
    - path: "workflows/spec.md"
      provides: "Complete Phase 4 (approval flow) replacing the placeholder from Plan 04-01"
      contains: "spec.json"
  key_links:
    - from: "workflows/spec.md"
      to: "./module_name/spec.json"
      via: "spec file output on approval"
      pattern: "spec\\.json"
---

<objective>
Complete the specification workflow's Phase 4 (Approval Flow) with markdown summary rendering, user review options, and spec.json git commit on approval.

Purpose: Enable users to review a human-readable summary of the inferred specification, make changes if needed, and explicitly approve before any generation begins. The approved spec.json becomes an auditable, version-controlled generation contract.

Output: Updated `workflows/spec.md` with complete Phase 4 replacing the placeholder
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-input-specification/04-RESEARCH.md
@.planning/phases/04-input-specification/04-CONTEXT.md
@.planning/phases/04-input-specification/04-01-SUMMARY.md

Reference:
@workflows/spec.md
@workflows/scaffold.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement approval flow in spec workflow Phase 4</name>
  <files>workflows/spec.md</files>
  <action>
Replace the Phase 4 placeholder in `workflows/spec.md` with the complete approval flow implementation. The Phase 4 section should be approximately 150-200 lines.

**## Phase 4: Present Specification for Approval**

### Step 4.1: Render Markdown Summary

Generate the spec.json FIRST (Phase 3 output), then render a markdown summary FROM the JSON. The markdown is a view of the JSON, not an independent document. This prevents spec-markdown desync.

The markdown summary format:

```markdown
## Module Specification: {module_title}

### Overview
| Property | Value |
|----------|-------|
| Technical Name | `{module_name}` |
| Title | {module_title} |
| Summary | {summary} |
| Category | {category} |
| License | {license} |
| Odoo Version | {odoo_version} |
| Application | {yes/no} |

### Dependencies
{depends list as comma-separated, e.g., `base`, `mail`, `stock`}

### Models

#### {model_description} (`{model_name}`)
{If _inherit is set: "Extends: `{_inherit}`"}
{If inherit_mixins: "Mixins: {list}"}

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| {name} | {type} | {Yes/No} | {default or -} | {help} |
| ... | ... | ... | ... | ... |

{If workflow_states:}
**Workflow States:** {state1} -> {state2} -> {state3}

{If constraints:}
**Constraints:**
- {constraint description}

{Repeat for each model}

### Relationships
{For each Many2one/One2many/Many2many field across all models:}
- `{model1}` -> `{model2}` via `{field_name}` ({field_type})

### Views
{For each model:}
- **{model_description}**: {view types joined by ", "} (e.g., Form, Tree, Search)

### Security Groups
| Group | Label | Permissions | Inherits From |
|-------|-------|-------------|---------------|
| {group_name} | {label} | {R/W/C/U summary} | {implied_ids or -} |

{If workflow_states exist for any model:}
### Workflow States
{For each model with states:}
**{model_description}:**
{State diagram as text: Draft -> Confirmed -> Done}
{Note which groups can trigger transitions, if specified}

### Demo Data
{demo_data_hints as bullet list}

### Inferred Defaults (Review These)
The following were inferred from your description and standard Odoo patterns:
{List every default that was inferred, not explicitly stated by user:}
- Field `{name}` on `{model}` inferred as {type} (reason: keyword "{keyword}" pattern)
- Dependency `{dep}` added because description mentions {trigger}
- Security groups set to default User/Manager hierarchy
{This section MUST be present and MUST list all inferences}
```

### Step 4.2: Present for User Review

After rendering the markdown summary, present it to the user with these options:

```
---

**Please review the specification above.**

Options:
1. **Approve** - Proceed with this specification (spec.json will be committed to git)
2. **Request Changes** - Tell me what to modify (I'll ask targeted follow-up questions)
3. **Edit Directly** - Describe your edits in plain text (I'll update the spec accordingly)
```

### Step 4.3: Handle User Response

**If user approves** (says "approve", "yes", "looks good", "1", "proceed", "confirm", "approved"):
1. Write spec.json to `./module_name/spec.json` (create directory if needed)
2. Commit to git:
   ```bash
   git add ./module_name/spec.json
   git commit -m "spec(module_name): approved module specification

   Module: module_title
   Models: model1, model2, ...
   "
   ```
3. Report:
   ```
   Specification approved and committed.

   Spec file: ./module_name/spec.json
   Commit: {git hash}

   Next steps:
   - To generate the module from this spec: /odoo-gen:new module_name
     (Once Phase 5 is active, generation will use this spec automatically)
   - To modify the spec later: /odoo-gen:plan module_name
   - To validate a generated module: /odoo-gen:validate ./module_name/
   ```

**If user requests changes** (says "changes", "modify", "2", "request changes", or describes what to change):
1. Parse which parts of the spec the user wants changed
2. Ask 1-3 TARGETED follow-up questions about only the changed sections:
   - If models change: "You want to [add/remove/rename] [model]. What fields should [model] have?"
   - If fields change: "For [model].[field], should it be [type]? Required? Any default value?"
   - If workflow changes: "What states should [model] have? What triggers each transition?"
   - If dependencies change: "Adding [dep] dependency. This gives access to [what]. Is that what you need?"
3. Update the spec.json based on user's answers
4. Re-render the markdown summary
5. Present for approval again (loop back to Step 4.2)

**If user edits directly** (says "edit", "3", or provides specific edits):
1. Parse the user's edit instructions
2. Apply changes directly to the spec.json
3. Re-render the markdown summary showing changes
4. Present for approval again (loop back to Step 4.2)

### Step 4.4: Iteration Limit

After 3 rounds of changes, suggest: "We've iterated 3 times. If the spec still needs significant changes, consider starting fresh with `/odoo-gen:plan` and a more detailed description."

### Error Handling

- If spec.json cannot be written (permission error, disk full): Report error, suggest alternative path
- If git commit fails: Report error, spec.json is still saved locally. User can commit manually.
- If user response is ambiguous: Ask for clarification. Default to "Request Changes" interpretation.

### Key Rules

1. **Generate JSON first, markdown from JSON**: Never maintain them independently
2. **Show ALL inferred defaults**: The "Inferred Defaults" section must list every assumption
3. **Approval blocks generation**: No downstream process can use the spec until it's approved and committed
4. **spec.json is the contract**: The markdown summary is for human review only. The JSON file is what code generation reads.
5. **Backward compatibility**: The spec.json MUST be loadable by `odoo-gen-utils render-module --spec-file`. New fields are optional with null/empty defaults.
  </action>
  <verify>
Read `workflows/spec.md` and verify Phase 4 is complete (not a placeholder). Check for:
- Markdown summary template with all sections (Overview, Dependencies, Models, Relationships, Views, Security Groups, Workflow States)
- "Inferred Defaults" section in the template
- Three user options (Approve, Request Changes, Edit Directly)
- spec.json write to ./module_name/spec.json
- Git commit step
- Change request handling (re-ask targeted questions)
- Iteration limit (3 rounds)

```bash
grep -c "Approve\|Request Changes\|Edit Directly" workflows/spec.md && grep -c "spec.json" workflows/spec.md && grep -c "Inferred Defaults" workflows/spec.md && grep -c "git commit\|git add" workflows/spec.md
```

Expected: 3+ (three options), 5+ (spec.json references), 1+ (inferred defaults), 2+ (git commands).
  </verify>
  <done>
Spec workflow Phase 4 is complete with: markdown summary rendering from spec.json (all sections present), three user options (Approve/Request Changes/Edit Directly), spec.json committed to git on approval, targeted follow-up on change requests, 3-round iteration limit, and "Inferred Defaults" section that shows all assumptions explicitly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete specification workflow for /odoo-gen:plan command. The workflow:
1. Parses natural language descriptions into draft module specs
2. Asks tiered Odoo-specific follow-up questions (3-5 Tier 1, 0-3 Tier 2 based on complexity)
3. Generates structured JSON spec (backward-compatible with render-module)
4. Renders human-readable markdown summary showing all inferred defaults
5. Presents approval flow with three options (Approve / Request Changes / Edit Directly)
6. Commits approved spec.json to git as generation contract

Files created/modified:
- workflows/spec.md (NEW -- complete specification pipeline)
- commands/plan.md (UPDATED -- active command, no longer stub)
- agents/odoo-scaffold.md (UPDATED -- dual-mode with KB references)
  </what-built>
  <how-to-verify>
1. Read `workflows/spec.md` and verify it defines all 4 phases with substantive content
2. Read `commands/plan.md` and verify it has `agent: odoo-scaffold` and references spec.md
3. Read `agents/odoo-scaffold.md` and verify dual-mode section exists and KB references are comprehensive
4. Check that the spec JSON schema in the workflow extends (not breaks) the existing render-module format
5. Verify the markdown summary template includes an "Inferred Defaults" section
6. Verify the approval flow has all three options and the git commit step
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the specification workflow is correct, or describe any issues to address.</resume-signal>
</task>

</tasks>

<verification>
1. `workflows/spec.md` Phase 4 is fully implemented (not a placeholder)
2. Markdown summary template includes all required sections from CONTEXT.md
3. "Inferred Defaults" section ensures no silent assumptions
4. Three approval options are present (Approve, Request Changes, Edit Directly)
5. spec.json is written to `./module_name/spec.json` and committed to git on approval
6. Change request handling loops back with targeted follow-up questions
7. The complete workflow (Phases 1-4) forms a coherent pipeline
</verification>

<success_criteria>
- User can review a human-readable markdown summary of the parsed specification (INPT-04)
- Summary explicitly shows all inferred defaults for transparency
- User has three options: approve, request changes, or edit directly
- On approval, spec.json is committed to git as the generation contract
- On change request, system asks targeted questions and re-presents
- The approval step blocks any downstream generation
</success_criteria>

<output>
After completion, create `.planning/phases/04-input-specification/04-02-SUMMARY.md`
</output>
