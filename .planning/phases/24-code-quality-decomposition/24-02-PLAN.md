---
phase: 24-code-quality-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/odoo_gen_utils/renderer.py
  - tests/test_render_stages.py
autonomous: true
requirements: [QUAL-02]

must_haves:
  truths:
    - "render_module orchestrator calls 7 stage functions instead of inlining all logic"
    - "Each stage function (render_manifest, render_models, render_views, render_security, render_wizards, render_tests, render_static) is under 80 lines"
    - "Each stage function returns Result[list[Path]] from Phase 23 types"
    - "render_module public API signature is unchanged — existing callers work without modification"
  artifacts:
    - path: "src/python/odoo_gen_utils/renderer.py"
      provides: "Decomposed renderer with 7 stage functions + orchestrator"
      contains: "def render_manifest"
    - path: "tests/test_render_stages.py"
      provides: "Unit tests for each decomposed stage function"
      contains: "test_render_manifest"
  key_links:
    - from: "src/python/odoo_gen_utils/renderer.py:render_module"
      to: "render_manifest, render_models, render_views, render_security, render_wizards, render_tests, render_static"
      via: "sequential stage function calls collecting Result[list[Path]]"
      pattern: "render_\\w+\\(env, spec, module_dir"
    - from: "src/python/odoo_gen_utils/renderer.py:render_*"
      to: "odoo_gen_utils.validation.types.Result"
      via: "return type"
      pattern: "Result\\[list\\[Path\\]\\]"
---

<objective>
Decompose render_module from a 371-line monolith into 7 independently testable stage functions.

Purpose: render_module is the largest function in the codebase (~376 lines). Decomposition improves maintainability, testability, and makes each rendering stage independently verifiable. Stage functions use Result[T] from Phase 23 for consistent error handling.
Output: 7 stage functions each under 80 lines, orchestrator under 60 lines, all returning Result types. Full backward compatibility.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-code-quality-decomposition/24-RESEARCH.md
@.planning/phases/24-code-quality-decomposition/24-VALIDATION.md
@src/python/odoo_gen_utils/renderer.py
@src/python/odoo_gen_utils/validation/types.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create stage function tests and extract render_manifest + render_models + render_views</name>
  <files>src/python/odoo_gen_utils/renderer.py, tests/test_render_stages.py</files>
  <behavior>
    - Test 1: render_manifest(env, spec, module_dir, module_context) returns Result with success=True and list of Path objects for __manifest__.py, __init__.py, models/__init__.py
    - Test 2: render_models(env, spec, module_dir, module_context, verifier) returns Result with success=True and list of Paths for each model .py file
    - Test 3: render_views(env, spec, module_dir, module_context) returns Result with success=True and list of Paths for menu XML
    - Test 4: Each extracted function is under 80 lines (inspect source)
    - Test 5: All existing test_renderer.py tests still pass (backward compatibility)
  </behavior>
  <action>
    1. Create tests/test_render_stages.py with tests for render_manifest, render_models, render_views. Use a minimal spec fixture (reuse patterns from existing test_renderer.py). Each test verifies Result.success is True and Result.data contains expected file paths.

    2. Extract render_manifest from render_module (lines ~352-390 approx):
       - Signature: `def render_manifest(env, spec, module_dir, module_context) -> Result[list[Path]]`
       - Renders __manifest__.py, root __init__.py, models/__init__.py
       - Returns Result.ok(created_files) on success, Result.fail(error_msg) on template error

    3. Extract render_models (lines ~390-542 approx):
       - Signature: `def render_models(env, spec, module_dir, module_context, verifier=None) -> Result[list[Path]]`
       - Per-model .py files, per-model views, actions
       - Pass verifier parameter (used for inline verification during model render)
       - Keep _build_model_context as shared helper (DO NOT move it into render_models)

    4. Extract render_views (lines ~542-560 approx):
       - Signature: `def render_views(env, spec, module_dir, module_context) -> Result[list[Path]]`
       - Menu XML rendering

    5. Update render_module to call these 3 stage functions, collecting results. Keep remaining logic inline for now (Task 2 extracts the rest).

    6. Import Result from odoo_gen_utils.validation.types at module level (it is lightweight).

    7. Run both test_render_stages.py AND test_renderer.py to ensure no regressions.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_render_stages.py tests/test_renderer.py -x -q</automated>
  </verify>
  <done>render_manifest, render_models, render_views extracted as stage functions under 80 lines each, returning Result[list[Path]]. render_module calls them. All renderer tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract render_security + render_wizards + render_tests + render_static and finalize orchestrator</name>
  <files>src/python/odoo_gen_utils/renderer.py, tests/test_render_stages.py</files>
  <behavior>
    - Test 1: render_security(env, spec, module_dir, module_context) returns Result with Paths for security.xml, ir.model.access.csv, record_rules.xml
    - Test 2: render_wizards(env, spec, module_dir, module_context) returns Result with Paths for wizard files
    - Test 3: render_tests(env, spec, module_dir, module_context) returns Result with Paths for test files
    - Test 4: render_static(env, spec, module_dir, module_context) returns Result with Paths for data.xml, static files, etc.
    - Test 5: All 7 stage functions are under 80 lines each
    - Test 6: render_module orchestrator is under 80 lines
    - Test 7: Full test_renderer.py suite still passes (backward compatibility)
  </behavior>
  <action>
    1. Add tests for render_security, render_wizards, render_tests, render_static to tests/test_render_stages.py. Add a line-count test that inspects all 7 stage functions.

    2. Extract render_security (lines ~560-600 approx):
       - Signature: `def render_security(env, spec, module_dir, module_context) -> Result[list[Path]]`
       - security.xml, ir.model.access.csv, record_rules.xml

    3. Extract render_wizards (lines ~600-650 approx):
       - Signature: `def render_wizards(env, spec, module_dir, module_context) -> Result[list[Path]]`
       - wizards/__init__.py, per-wizard .py, per-wizard form XML

    4. Extract render_tests (lines ~650-680 approx):
       - Signature: `def render_tests(env, spec, module_dir, module_context) -> Result[list[Path]]`
       - tests/__init__.py, per-model test files

    5. Extract render_static (lines ~680-728 approx):
       - Signature: `def render_static(env, spec, module_dir, module_context) -> Result[list[Path]]`
       - data.xml, sequences.xml, demo data, static/index.html, README.rst

    6. Finalize render_module orchestrator:
       - Setup (~20 lines: env, module_dir, contexts, state)
       - Loop over [render_manifest, render_models, render_views, render_security, render_wizards, render_tests, render_static]
       - For each: call stage, check result.success, short-circuit on failure, extend created_files with result.data
       - Keep state tracking (_state) in orchestrator only — stage functions do NOT touch _state
       - render_models gets verifier parameter; others do not need it
       - Orchestrator should be under 80 lines

    7. Verify render_module public API is unchanged: same parameters, same return type (created_files, warnings).

    8. Run full test suite.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_render_stages.py tests/test_renderer.py -x -q</automated>
  </verify>
  <done>All 7 stage functions extracted and under 80 lines. Orchestrator under 80 lines. All return Result[list[Path]]. render_module public API unchanged. Full test suite green.</done>
</task>

</tasks>

<verification>
cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/ -x -q --ignore=tests/test_golden_path.py
</verification>

<success_criteria>
- render_module decomposed into 7 stage functions: render_manifest, render_models, render_views, render_security, render_wizards, render_tests, render_static
- Each stage function is independently testable and under 80 lines
- Each stage function returns Result[list[Path]]
- render_module orchestrator is under 80 lines and calls stage functions in sequence
- render_module public API (signature + return type) unchanged — no breaking changes for callers
- _build_model_context remains a shared helper, not duplicated
- Full test suite passes including existing test_renderer.py
</success_criteria>

<output>
After completion, create `.planning/phases/24-code-quality-decomposition/24-02-SUMMARY.md`
</output>
