---
phase: 24-code-quality-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/python/odoo_gen_utils/cli.py
  - src/python/odoo_gen_utils/validation/docker_runner.py
  - src/python/odoo_gen_utils/data/docker-compose.yml
  - python/pyproject.toml
  - tests/test_cli_lazy_imports.py
  - tests/test_docker_compose_path.py
autonomous: true
requirements: [QUAL-01, QUAL-03]

must_haves:
  truths:
    - "CLI module loads without importing chromadb, PyGithub, gitpython, Docker, or validation stack"
    - "Each CLI command still works correctly with its lazy imports"
    - "Docker compose file is resolved via importlib.resources with env var override, not parent traversal"
  artifacts:
    - path: "src/python/odoo_gen_utils/cli.py"
      provides: "CLI with lazy imports inside command functions"
      contains: "from odoo_gen_utils"
    - path: "src/python/odoo_gen_utils/validation/docker_runner.py"
      provides: "get_compose_file using importlib.resources"
      contains: "importlib.resources"
    - path: "src/python/odoo_gen_utils/data/docker-compose.yml"
      provides: "Docker compose as package data"
  key_links:
    - from: "src/python/odoo_gen_utils/cli.py"
      to: "odoo_gen_utils submodules"
      via: "lazy imports inside each @main.command() function"
      pattern: "def \\w+\\(.*\\):.*\\n.*from odoo_gen_utils"
    - from: "src/python/odoo_gen_utils/validation/docker_runner.py"
      to: "odoo_gen_utils/data/docker-compose.yml"
      via: "importlib.resources.files()"
      pattern: "files.*odoo_gen_utils.*joinpath.*docker-compose"
---

<objective>
Lazy CLI imports and robust Docker compose path resolution.

Purpose: CLI startup is slow because it imports heavy libraries (chromadb, PyGithub, etc.) at module level even when not needed. Docker compose path uses fragile 5-level parent traversal that breaks if package structure changes.
Output: Fast CLI startup with lazy imports; Docker compose resolved via importlib.resources + env var override.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-code-quality-decomposition/24-RESEARCH.md
@.planning/phases/24-code-quality-decomposition/24-VALIDATION.md
@src/python/odoo_gen_utils/cli.py
@src/python/odoo_gen_utils/validation/docker_runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI lazy imports — move heavy imports inside command functions</name>
  <files>src/python/odoo_gen_utils/cli.py, tests/test_cli_lazy_imports.py</files>
  <behavior>
    - Test 1: Importing cli module does NOT trigger import of chromadb, PyGithub, gitpython, docker, or validation submodules
    - Test 2: `python -c "from odoo_gen_utils.cli import main"` succeeds without loading heavy deps (check sys.modules)
    - Test 3: Each command function (validate, render, build_index, search_modules_cmd, etc.) still imports its needed modules when called
  </behavior>
  <action>
    1. First create tests/test_cli_lazy_imports.py:
       - Test that after `from odoo_gen_utils.cli import main`, sys.modules does NOT contain chromadb, github, git, docker, odoo_gen_utils.auto_fix, odoo_gen_utils.search, odoo_gen_utils.validation, odoo_gen_utils.renderer, odoo_gen_utils.i18n_extractor, odoo_gen_utils.edition, odoo_gen_utils.kb_validator
       - Use subprocess to run `python -c "from odoo_gen_utils.cli import main; import sys; heavy = [m for m in sys.modules if any(h in m for h in ['chromadb','github','docker','gitpython'])]; assert not heavy, f'Heavy modules loaded: {heavy}'"` to verify in a clean process

    2. Then refactor cli.py module-level imports:
       - KEEP at module level: click, json, sys, Path (from pathlib), __version__
       - MOVE into respective command functions per the research import mapping:
         - `auto_fix.*` -> inside `validate` command
         - `i18n_extractor.*` -> inside `extract_i18n` command
         - `kb_validator.*` -> inside `validate_kb` command
         - `search.*` (all submodules) -> inside `build_index`, `index_status`, `search_modules_cmd`, `extend_module_cmd`
         - `edition.*` -> inside `check_edition` command
         - `renderer.*` -> inside `render`, `list_templates`, `render_module_cmd`
         - `verifier.*` -> inside `render_module_cmd`
         - `validation.*` -> inside `validate` command

    3. Run tests to verify lazy imports work and no circular import issues arise.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_cli_lazy_imports.py tests/test_cli_validate.py -x -q</automated>
  </verify>
  <done>CLI module-level imports contain only click and stdlib. Heavy libraries load only when their respective commands are invoked. All existing CLI tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Docker compose path via importlib.resources with env var override</name>
  <files>src/python/odoo_gen_utils/validation/docker_runner.py, src/python/odoo_gen_utils/data/docker-compose.yml, python/pyproject.toml, tests/test_docker_compose_path.py</files>
  <behavior>
    - Test 1: get_compose_file() returns a Path that exists and ends with docker-compose.yml
    - Test 2: When ODOO_GEN_COMPOSE_FILE env var is set, get_compose_file() returns that path instead
    - Test 3: get_compose_file() does NOT use Path(__file__).parent.parent pattern (inspect source)
  </behavior>
  <action>
    1. First create tests/test_docker_compose_path.py with the behavior tests above.

    2. Copy docker/docker-compose.yml to src/python/odoo_gen_utils/data/docker-compose.yml (create data/ dir if needed under the top-level package, not under validation/).

    3. Update pyproject.toml to include package data files (ensure odoo_gen_utils/data/ is included in wheel builds).

    4. Refactor get_compose_file() in docker_runner.py:
       ```python
       import os
       from importlib.resources import files
       from pathlib import Path

       def get_compose_file() -> Path:
           env_path = os.environ.get("ODOO_GEN_COMPOSE_FILE")
           if env_path:
               return Path(env_path)
           ref = files("odoo_gen_utils").joinpath("data", "docker-compose.yml")
           return Path(str(ref))
       ```

    5. Remove the old 5-level parent traversal code.

    6. Pitfall: importlib.resources returns Traversable, not Path. Use Path(str(ref)) as recommended in research.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_docker_compose_path.py tests/test_docker_runner.py -x -q</automated>
  </verify>
  <done>get_compose_file() uses importlib.resources with ODOO_GEN_COMPOSE_FILE env var override. No parent directory traversal remains. All docker_runner tests pass.</done>
</task>

</tasks>

<verification>
cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_cli_lazy_imports.py tests/test_docker_compose_path.py tests/test_cli_validate.py tests/test_docker_runner.py -x -q
</verification>

<success_criteria>
- `python -c "from odoo_gen_utils.cli import main"` completes without loading chromadb, PyGithub, gitpython, or docker
- get_compose_file() returns valid path via importlib.resources, respects ODOO_GEN_COMPOSE_FILE env var
- No `.parent.parent.parent` traversal in docker_runner.py
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/24-code-quality-decomposition/24-01-SUMMARY.md`
</output>
