---
phase: 31-reports-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/src/odoo_gen_utils/renderer.py
  - python/src/odoo_gen_utils/templates/shared/report_action.xml.j2
  - python/src/odoo_gen_utils/templates/shared/report_template.xml.j2
  - python/src/odoo_gen_utils/templates/shared/graph_view.xml.j2
  - python/src/odoo_gen_utils/templates/shared/pivot_view.xml.j2
  - python/src/odoo_gen_utils/templates/17.0/view_form.xml.j2
  - python/src/odoo_gen_utils/templates/17.0/action.xml.j2
  - python/src/odoo_gen_utils/templates/18.0/view_form.xml.j2
  - python/src/odoo_gen_utils/templates/18.0/action.xml.j2
  - python/tests/test_render_stages.py
  - python/tests/test_renderer.py
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02

must_haves:
  truths:
    - "A spec with reports entries generates ir.actions.report XML with binding_model_id and QWeb template with t-foreach/t-field"
    - "A spec with reports and paper_format generates a report.paperformat XML record"
    - "Form views for models with reports get a print button in the header"
    - "A spec with dashboards entries generates graph view XML with measures/dimensions and pivot view XML with row/col/measure"
    - "Action view_mode includes graph,pivot when model has dashboard entries"
    - "Report and dashboard data files appear in the module manifest"
    - "render_reports returns Result.ok([]) when no reports or dashboards in spec"
  artifacts:
    - path: "python/src/odoo_gen_utils/templates/shared/report_action.xml.j2"
      provides: "ir.actions.report + optional paper format XML"
    - path: "python/src/odoo_gen_utils/templates/shared/report_template.xml.j2"
      provides: "QWeb t-call/t-foreach/t-field report body"
    - path: "python/src/odoo_gen_utils/templates/shared/graph_view.xml.j2"
      provides: "Graph view with measures and dimensions"
    - path: "python/src/odoo_gen_utils/templates/shared/pivot_view.xml.j2"
      provides: "Pivot view with row/col/measure fields"
    - path: "python/src/odoo_gen_utils/renderer.py"
      provides: "render_reports() implementation + context enrichment"
      contains: "def render_reports"
  key_links:
    - from: "renderer.py::render_reports()"
      to: "templates/shared/report_action.xml.j2"
      via: "render_template() call"
      pattern: "render_template.*report_action"
    - from: "renderer.py::_build_module_context()"
      to: "__manifest__.py data list"
      via: "data_files append for report/dashboard files"
      pattern: "report_.*\\.xml|graph\\.xml|pivot\\.xml"
    - from: "renderer.py::_build_model_context()"
      to: "view_form.xml.j2 and action.xml.j2"
      via: "model_reports and has_dashboard context vars"
      pattern: "model_reports|has_dashboard"
---

<objective>
Implement QWeb report generation and graph/pivot dashboard views in the render_reports pipeline stage.

Purpose: Replace the Phase 30 placeholder render_reports() with real logic that generates PDF report templates, paper formats, print buttons, and analytics dashboard views from spec declarations.
Output: 4 new Jinja2 templates, render_reports() implementation, context enrichment in _build_module_context() and _build_model_context(), modified form view and action templates, and comprehensive tests.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-reports-analytics/31-RESEARCH.md
@.planning/phases/31-reports-analytics/31-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Jinja2 templates + render_reports() implementation + context enrichment</name>
  <files>
    python/src/odoo_gen_utils/templates/shared/report_action.xml.j2,
    python/src/odoo_gen_utils/templates/shared/report_template.xml.j2,
    python/src/odoo_gen_utils/templates/shared/graph_view.xml.j2,
    python/src/odoo_gen_utils/templates/shared/pivot_view.xml.j2,
    python/src/odoo_gen_utils/renderer.py,
    python/src/odoo_gen_utils/templates/17.0/view_form.xml.j2,
    python/src/odoo_gen_utils/templates/17.0/action.xml.j2,
    python/src/odoo_gen_utils/templates/18.0/view_form.xml.j2,
    python/src/odoo_gen_utils/templates/18.0/action.xml.j2,
    python/tests/test_render_stages.py,
    python/tests/test_renderer.py
  </files>
  <behavior>
    TMPL-01 Tests (TestRenderReports class in test_render_stages.py):
    - test_report_generates_action_xml: spec with reports entry -> render_reports() creates data/report_{xml_id}.xml containing ir.actions.report record
    - test_report_action_fields: generated report action has binding_model_id, report_name matching template id, report_type=qweb-pdf, binding_type=report
    - test_report_qweb_template: generated QWeb template has t-call="web.html_container", t-foreach="docs", t-call="web.external_layout", t-field for columns, class="page" div
    - test_report_paper_format: spec with paper_format -> generates report.paperformat record with format/orientation/margins; without paper_format -> no paperformat record
    - test_form_print_button: model with reports -> form view XML contains button with name="%(report_{module}_{xml_id})d" type="action"
    - test_no_reports_noop: spec without reports or dashboards -> render_reports returns Result.ok([]) with empty list

    TMPL-02 Tests (TestRenderDashboards class in test_render_stages.py):
    - test_graph_view: spec with dashboards -> generates ir.ui.view with graph element, chart_type attribute, dimension fields, measure fields with type="measure"
    - test_graph_measures: graph view measure fields have type="measure"; dimension fields with interval get interval attribute
    - test_pivot_view: generates ir.ui.view with pivot element, row fields type="row", col fields type="col", measure fields type="measure"
    - test_action_view_mode: model with dashboard -> action.xml view_mode includes "graph,pivot"; model without dashboard -> view_mode is "tree,form" only
    - test_no_dashboards_noop: spec without dashboards -> no graph/pivot files generated

    Context Tests (in test_renderer.py):
    - TestBuildModuleContextReports: reports in spec -> data_files includes report action and template XML paths
    - TestBuildModuleContextDashboards: dashboards in spec -> view files include graph/pivot XML paths
    - TestBuildModelContextReports: model with reports -> model context has model_reports list and has_dashboard bool
  </behavior>
  <action>
    **Step 1: Write failing tests first (RED)**

    In test_render_stages.py, add TestRenderReports and TestRenderDashboards classes. Use the existing _make_spec() helper pattern. Create test specs with reports and dashboards entries following the schema from 31-RESEARCH.md Pattern 9. Each test calls render_reports() or render_module() and asserts on file existence and XML content.

    In test_renderer.py, add TestBuildModuleContextReports, TestBuildModuleContextDashboards, and TestBuildModelContextReports classes testing that _build_module_context() and _build_model_context() include report/dashboard data in their return dicts.

    Run tests -- they MUST fail (templates don't exist, render_reports is placeholder).

    **Step 2: Create 4 Jinja2 templates (GREEN)**

    Create `templates/shared/report_action.xml.j2`:
    - XML declaration + `<odoo>` wrapper
    - `ir.actions.report` record with: name, model, report_type=qweb-pdf, report_name={module_name}.report_{xml_id}, binding_model_id ref, binding_type=report
    - Conditional paperformat_id ref when report.paper_format exists
    - Conditional `report.paperformat` record when report.paper_format exists (format, orientation, margins with defaults: A4, Portrait, 20/20/7/7/35)

    Create `templates/shared/report_template.xml.j2`:
    - XML declaration + `<odoo>` wrapper
    - `<template id="report_{xml_id}">` matching report_name
    - t-call="web.html_container" > t-foreach="docs" t-as="doc" > t-call="web.external_layout"
    - div class="page" with h2 t-field="doc.display_name"
    - Table with thead from report.columns labels
    - If report.detail_field: t-foreach on doc.{detail_field} with t-field per column
    - Else: simple t-field rows for each column

    Create `templates/shared/graph_view.xml.j2`:
    - ir.ui.view record with graph arch
    - graph string from dashboard.title (default: model description + " Analysis")
    - type from dashboard.chart_type (default: "bar")
    - Conditional stacked="True"
    - Dimension fields (with optional interval attribute)
    - Measure fields with type="measure"

    Create `templates/shared/pivot_view.xml.j2`:
    - ir.ui.view record with pivot arch
    - pivot string, disable_linking="True", sample="1"
    - Row fields with type="row" (optional interval)
    - Col fields with type="col" (optional interval)
    - Measure fields with type="measure"

    **Step 3: Implement render_reports() in renderer.py**

    Replace the placeholder `render_reports()` (around line 1229) with real implementation:
    - Get reports = spec.get("reports", []) and dashboards = spec.get("dashboards", [])
    - If neither, return Result.ok([])
    - For each report: render report_action.xml.j2 to data/report_{xml_id}.xml, render report_template.xml.j2 to data/report_{xml_id}_template.xml
    - For each dashboard: render graph_view.xml.j2 to views/{model_xml_id}_graph.xml, render pivot_view.xml.j2 to views/{model_xml_id}_pivot.xml
    - Return Result.ok(created_files)

    **Step 4: Enrich _build_module_context()**

    Add report data files to data_files: for each report, append "data/report_{xml_id}.xml" and "data/report_{xml_id}_template.xml".
    Add dashboard view files: for each dashboard, append graph and pivot XML paths to view files list.

    **Step 5: Enrich _build_model_context()**

    Add model_reports: list of reports targeting this model (filter spec.get("reports", []) by model_name match).
    Add has_dashboard: bool indicating if any dashboard targets this model.

    **Step 6: Modify view_form.xml.j2 (both 17.0 and 18.0)**

    Extend the header condition: `{% if state_field or model_reports %}` (not just state_field).
    Inside header, after existing buttons, add print buttons:
    ```
    {% for report in model_reports %}
    <button name="%(report_{{ module_name }}_{{ report.xml_id }})d" string="{{ report.button_label | default('Print') }}" type="action" class="btn-secondary"/>
    {% endfor %}
    ```

    **Step 7: Modify action.xml.j2 (both 17.0 and 18.0)**

    Change view_mode line to: `tree,form{% if has_dashboard %},graph,pivot{% endif %}`

    **Step 8: Run tests -- all MUST pass (GREEN)**

    Run: `cd python && uv run pytest tests/test_render_stages.py tests/test_renderer.py -x -q -k 'report or graph or pivot or dashboard'`
    Then full suite: `cd python && uv run pytest -x -q`
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_render_stages.py tests/test_renderer.py -x -q</automated>
  </verify>
  <done>
    - render_reports() generates report action XML, QWeb template, optional paper format for each spec report
    - render_reports() generates graph and pivot view XML for each spec dashboard
    - Form views include print buttons for models with reports
    - Action view_mode includes graph,pivot for models with dashboards
    - Report and dashboard files appear in module manifest data_files
    - render_reports() returns Result.ok([]) when no reports/dashboards
    - All new tests pass, full test suite green
  </done>
</task>

</tasks>

<verification>
1. `cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_render_stages.py -x -q -k 'report or graph or pivot or dashboard'` -- all report/dashboard tests pass
2. `cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest tests/test_renderer.py -x -q -k 'Report or Dashboard'` -- context enrichment tests pass
3. `cd /home/inshal-rauf/Odoo_module_automation/python && uv run pytest -x -q` -- full suite green (no regressions)
</verification>

<success_criteria>
- 4 new Jinja2 templates exist in templates/shared/
- render_reports() replaces placeholder with working report + dashboard generation
- _build_module_context() adds report/dashboard files to manifest
- _build_model_context() provides model_reports and has_dashboard to templates
- view_form.xml.j2 renders print buttons when model has reports
- action.xml.j2 appends graph,pivot to view_mode when model has dashboards
- All tests pass including full regression suite
</success_criteria>

<output>
After completion, create `.planning/phases/31-reports-analytics/31-01-SUMMARY.md`
</output>
