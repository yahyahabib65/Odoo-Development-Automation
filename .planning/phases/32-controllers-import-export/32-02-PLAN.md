---
phase: 32-controllers-import-export
plan: 02
type: tdd
wave: 2
depends_on: [32-01]
files_modified:
  - python/src/odoo_gen_utils/templates/shared/import_wizard.py.j2
  - python/src/odoo_gen_utils/templates/shared/import_wizard_form.xml.j2
  - python/src/odoo_gen_utils/renderer.py
  - python/tests/test_render_stages.py
  - python/tests/test_renderer.py
autonomous: true
requirements: [TMPL-04]

must_haves:
  truths:
    - "A spec with import_export:true on a model generates a TransientModel import wizard"
    - "Import wizard has fields.Binary upload, state machine (upload/preview/done), and error_log"
    - "Import wizard validates file content type via magic bytes (PK\\x03\\x04), not just extension"
    - "Import wizard has preview step (action_preview) and batch _do_import method"
    - "Export action produces xlsx output via openpyxl with field headers"
    - "Import wizard form view has multi-state visibility (upload/preview/done groups)"
    - "external_dependencies includes openpyxl when any model has import_export"
    - "Import wizard gets ACL entry in security CSV"
  artifacts:
    - path: "python/src/odoo_gen_utils/templates/shared/import_wizard.py.j2"
      provides: "Import/export TransientModel with Binary upload, magic byte validation, preview, batch import, xlsx export"
      contains: "TransientModel"
    - path: "python/src/odoo_gen_utils/templates/shared/import_wizard_form.xml.j2"
      provides: "Wizard form view with state-dependent groups and export button + action window"
      contains: "ir.actions.act_window"
    - path: "python/src/odoo_gen_utils/renderer.py"
      provides: "render_controllers() extended with import/export wizard generation + manifest enrichment"
      contains: "import_export"
  key_links:
    - from: "renderer.py::render_controllers()"
      to: "import_wizard.py.j2"
      via: "render_template for each model with import_export"
      pattern: "render_template.*import_wizard\\.py\\.j2"
    - from: "renderer.py::_build_module_context()"
      to: "manifest.py.j2"
      via: "external_dependencies with openpyxl"
      pattern: "external_dependencies|openpyxl"
    - from: "renderer.py::render_controllers()"
      to: "import_wizard_form.xml.j2"
      via: "render_template for wizard form view"
      pattern: "render_template.*import_wizard_form"
---

<objective>
Implement import/export TransientModel wizard generation: render_controllers() is extended to produce import wizard Python files and form view XML for each model with import_export:true, with magic byte file validation, preview step, batch import, and xlsx export.

Purpose: TMPL-04 -- generated modules need bulk data import/export capabilities with file validation and user-friendly preview workflow.
Output: 2 new Jinja2 templates, extended render_controllers(), manifest enrichment (external_dependencies, wizard files), and tests.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-controllers-import-export/32-RESEARCH.md
@.planning/phases/32-controllers-import-export/32-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (render_controllers now implemented): -->

From python/src/odoo_gen_utils/renderer.py (after Plan 01):
```python
def render_controllers(
    env: Environment,
    spec: dict[str, Any],
    module_dir: Path,
    module_context: dict[str, Any],
) -> "Result[list[Path]]":
    # Now generates controllers/main.py and controllers/__init__.py
    # Needs extension: also generate import wizard files for models with import_export

def _build_module_context(spec, module_name) -> dict:
    # Now includes has_controllers
    # Needs extension: has_import_export, external_dependencies, import wizard files in manifest

# Helper functions:
def _to_python_var(name: str) -> str   # "academy.course" -> "academy_course"
def _to_xml_id(name: str) -> str       # "academy.course" -> "academy_course"
def _to_class(name: str) -> str        # "academy.course" -> "AcademyCourse"
```

Import/export spec schema:
```python
# In spec["models"], each model can have:
{
    "name": "academy.course",
    "import_export": true,   # triggers wizard generation
    "fields": [
        {"name": "name", "type": "Char", "required": true},
        {"name": "code", "type": "Char"},
        {"name": "credits", "type": "Integer"},
    ]
}
```

From RESEARCH.md -- import wizard places files in wizards/ directory (consistent with existing wizard pattern):
- wizards/{model_var}_import_wizard.py
- views/{model_xml_id}_import_wizard_form.xml

Existing wizard rendering pattern (render_wizards):
```python
# init_wizards.py.j2 renders from . import {wizard_var}
# wizard.py.j2 renders TransientModel class
# wizard_form.xml.j2 renders form view + action
# wizard form files go to views/ directory
# wizard ACL entries generated in render_security()
```

Existing init_wizards.py.j2:
```jinja2
{# init_wizards.py.j2 #}
{% for wizard in spec_wizards %}
from . import {{ wizard | replace('.', '_') }}
{% endfor %}
```

Existing access_csv.j2 (security ACL template) -- need to understand how wizard ACLs are added.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Import/export wizard templates + render_controllers extension</name>
  <files>
    python/src/odoo_gen_utils/templates/shared/import_wizard.py.j2
    python/src/odoo_gen_utils/templates/shared/import_wizard_form.xml.j2
    python/src/odoo_gen_utils/renderer.py
    python/tests/test_render_stages.py
    python/tests/test_renderer.py
  </files>
  <behavior>
    - test_import_wizard_generated: spec with model having import_export:true -> generates wizards/{model_var}_import_wizard.py
    - test_import_wizard_fields: generated wizard has fields.Binary, state Selection (upload/preview/done), preview_html, import_count, error_log
    - test_content_type_validation: generated wizard has _validate_file_content method checking magic bytes b'PK\x03\x04'
    - test_preview_and_batch_import: generated wizard has action_preview(), action_import(), _do_import(), _parse_row() methods
    - test_export_xlsx: generated wizard has action_export() method referencing openpyxl.Workbook() with field headers
    - test_wizard_form_states: generated form XML has 3 groups with state-dependent invisible attrs (upload/preview/done) and action buttons
    - TestBuildModuleContextImportExport: _build_module_context sets has_import_export=True and external_dependencies includes openpyxl when any model has import_export; wizard form files appear in manifest
    - test_import_wizard_security: import wizard model gets ACL entry (either in render_controllers output or render_security picks it up)
    - test_no_import_export_noop: model without import_export:true -> no wizard files generated
    - test_import_wizard_init: wizards/__init__.py imports import wizard modules (extend init_wizards.py.j2 or handle in render_controllers)
  </behavior>
  <action>
    RED phase -- write failing tests in test_render_stages.py (class TestRenderImportExport) and test_renderer.py (class TestBuildModuleContextImportExport). Use specs with models that have `"import_export": true` and verify generated output contains expected patterns. Run tests -- they must FAIL.

    GREEN phase -- implement:

    1. Create `templates/shared/import_wizard.py.j2`: Jinja2 template producing a TransientModel class:
       - `_name = '{{ model_name }}.import.wizard'`
       - `_description = 'Import {{ model_description }}'`
       - Fields: `state` (Selection upload/preview/done), `file_data` (Binary), `file_name` (Char), `preview_html` (Html), `import_count` (Integer), `error_log` (Text)
       - `_validate_file_content()`: base64 decode, check magic bytes `b'PK\x03\x04'`, raise ValidationError on mismatch
       - `action_preview()`: decode file, `import openpyxl` (inside method), load_workbook, build HTML table from first 6 rows, write state='preview'
       - `action_import()`: decode file, load_workbook, call `_do_import(rows)`
       - `_do_import(rows)`: per-row try/except with error collection, return (errors, count)
       - `_parse_row(row)`: stub with TODO, returns dict mapping field names
       - `action_export()`: `import openpyxl`, create Workbook, headers from model fields (non-relational, non-internal), data rows from `self.env['{{ model_name }}'].search([])`, base64 encode, return download URL via `/web/content`
       - `_reopen_wizard()`: return act_window action for self
       - Imports: `base64`, `io`, `from odoo import api, fields, models`, `from odoo.exceptions import UserError, ValidationError`
       - Use `dict.get()` for optional template variables (StrictUndefined safety)

    2. Create `templates/shared/import_wizard_form.xml.j2`: XML template with:
       - `ir.ui.view` record for form view with state-dependent groups (`invisible="state != 'upload'"` etc.)
       - Footer buttons: Preview (upload state), Import (preview state), Export All (always), Close
       - `ir.actions.act_window` record for the wizard action

    3. Extend `renderer.py::render_controllers()`: After controller file generation, iterate over `spec.get("models", [])` and for each model with `model.get("import_export")`:
       - Derive model_var, model_xml_id, model_class, model_description
       - Build context with module_context + model-specific keys + export_fields (non-relational fields for headers)
       - Render `import_wizard.py.j2` -> `module_dir / "wizards" / f"{model_var}_import_wizard.py"`
       - Render `import_wizard_form.xml.j2` -> `module_dir / "views" / f"{model_xml_id}_import_wizard_form.xml"`
       - Ensure wizards/__init__.py is updated to import the wizard module (either extend init_wizards.py.j2 context or render a separate init for import wizards)

    4. Extend `renderer.py::_build_module_context()`:
       - Compute `has_import_export = any(m.get("import_export") for m in models)`
       - Add `"has_import_export": has_import_export` to context
       - When has_import_export: add `"external_dependencies": {"python": ["openpyxl"]}` to context
       - Add import wizard form view files to manifest_files (after wizard_view_files section)
       - Ensure `has_wizards` is also True when import_export models exist (so wizards/ dir gets created)

    5. Handle security ACL: Either extend the security CSV template context to include import wizard models, or add ACL entries in render_controllers. Follow the pattern used by existing render_security() for wizard ACLs. The import wizard needs an entry like: `access_{model_xml_id}_import_wizard,{model_name}.import.wizard,model_{model_xml_id}_import_wizard,base.group_user,1,1,1,0`

    Run tests -- all must PASS.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && .venv/bin/python -m pytest tests/test_render_stages.py::TestRenderImportExport tests/test_renderer.py::TestBuildModuleContextImportExport -x -q</automated>
  </verify>
  <done>
    - import_wizard.py.j2 renders TransientModel with Binary upload, magic byte validation, preview, batch import, xlsx export
    - import_wizard_form.xml.j2 renders multi-state form view with action buttons
    - render_controllers() generates wizard files for models with import_export:true
    - _build_module_context includes has_import_export, external_dependencies with openpyxl
    - Import wizard ACL entries generated
    - All new tests pass, no regressions
  </done>
</task>

<task type="auto">
  <name>Task 2: Full suite regression + integration verification</name>
  <files></files>
  <action>
    Run the full test suite to confirm zero regressions from the import/export wizard changes. Verify that:
    - All existing render stages still work (manifest, models, views, security, wizards, tests, static, cron, reports, controllers)
    - The render_module pipeline correctly handles specs with both controllers AND import_export models
    - Wizard-related tests (TestRenderWizards) still pass with the extended has_wizards logic

    Expected: All 660+ tests pass (original 651 + Plan 01 controller tests + Plan 02 import/export tests).
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && .venv/bin/python -m pytest tests/ -x -q --ignore=tests/test_docker_integration.py --ignore=tests/test_golden_path.py --ignore=tests/test_verifier_integration.py</automated>
  </verify>
  <done>Full test suite passes with zero regressions. Both controller and import/export tests included in count.</done>
</task>

</tasks>

<verification>
- `cd /home/inshal-rauf/Odoo_module_automation/python && .venv/bin/python -m pytest tests/test_render_stages.py::TestRenderImportExport tests/test_renderer.py::TestBuildModuleContextImportExport -x -v` -- all import/export tests pass
- `cd /home/inshal-rauf/Odoo_module_automation/python && .venv/bin/python -m pytest tests/ -x -q --ignore=tests/test_docker_integration.py --ignore=tests/test_golden_path.py --ignore=tests/test_verifier_integration.py` -- full suite green
- grep for `PK\\x03\\x04` in import_wizard.py.j2 confirms magic byte validation
- grep for `openpyxl` in import_wizard.py.j2 confirms xlsx handling (inside methods, not top-level)
- grep for `external_dependencies` in renderer.py confirms manifest enrichment
</verification>

<success_criteria>
- render_controllers() generates import wizard files for models with import_export:true
- Import wizard template has Binary upload, magic byte validation, preview, batch import, xlsx export
- Form view has state-dependent visibility and action buttons
- Manifest includes external_dependencies with openpyxl
- Import wizard ACL entries present in security output
- All tests pass including new TestRenderImportExport class
</success_criteria>

<output>
After completion, create `.planning/phases/32-controllers-import-export/32-02-SUMMARY.md`
</output>
