---
phase: 20-auto-fix-ast-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - python/src/odoo_gen_utils/auto_fix.py
  - python/tests/test_auto_fix.py
autonomous: true
requirements:
  - AFIX-02

must_haves:
  truths:
    - "Any imported name with zero references in the file body is detected as unused and removed"
    - "Imported names that ARE referenced anywhere in the file body are preserved"
    - "Star imports (from X import *) are never removed"
    - "Names listed in __all__ are treated as used"
    - "Comments and whitespace formatting are preserved after import removal"
    - "Attribute access (e.g., api.constrains) correctly counts api as used"
  artifacts:
    - path: "python/src/odoo_gen_utils/auto_fix.py"
      provides: "Full AST body scan in fix_unused_imports replacing _IMPORT_USAGE_PATTERNS whitelist"
      contains: "_find_all_name_references"
    - path: "python/tests/test_auto_fix.py"
      provides: "Tests for arbitrary unused import detection, star import preservation, attribute chain handling"
      contains: "arbitrary"
  key_links:
    - from: "fix_unused_imports"
      to: "_find_all_name_references"
      via: "function call to collect all ast.Name nodes"
      pattern: "_find_all_name_references"
    - from: "fix_unused_imports"
      to: "ast.ImportFrom"
      via: "AST walk to find import nodes and compare against used names"
      pattern: "ast\\.ImportFrom|ast\\.Import"
---

<objective>
Replace the 4-name hardcoded whitelist in fix_unused_imports with a full AST body scan that detects ALL unused imports regardless of name.

Purpose: Current implementation only checks `api`, `ValidationError`, `AccessError`, `_` for usage. Any other unused import (e.g., `fields`, `Command`, custom helpers) silently passes through. Full AST scan eliminates all false negatives.

Output: fix_unused_imports uses ast.walk() + ast.Name to find all name references in file body, compares against imported names, removes any with zero references.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-auto-fix-ast-migration/20-RESEARCH.md
@.planning/phases/20-auto-fix-ast-migration/20-VALIDATION.md
@.planning/phases/20-auto-fix-ast-migration/20-01-SUMMARY.md

@python/src/odoo_gen_utils/auto_fix.py
@python/tests/test_auto_fix.py

<interfaces>
<!-- After Plan 01, the file will have AST splice utilities available -->
<!-- fix_unused_imports signature to preserve: -->
```python
def fix_unused_imports(file_path: Path) -> bool:
    """Detect and remove unused imports in a generated Python file."""
```

<!-- Current whitelist to DELETE: -->
```python
_IMPORT_USAGE_PATTERNS: dict[str, tuple[str, ...]] = {
    "api": ("@api.", "api."),
    "ValidationError": ("ValidationError",),
    "AccessError": ("AccessError",),
    "_": ("_(",),
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 tests for full-body import scanning</name>
  <files>python/tests/test_auto_fix.py</files>
  <behavior>
    - TestUnusedImportsArbitraryNames: importing `fields` but never using it -> detected and removed
    - TestUnusedImportsArbitraryNames: importing `Command` but never using it -> detected and removed
    - TestUnusedImportsArbitraryNames: importing `fields` AND using `fields.Char(...)` -> kept (attribute access counts as usage)
    - TestUnusedImportsStarImport: `from odoo import *` is never removed even if no explicit name references found
    - TestUnusedImportsAllExport: name in `__all__` list treated as used even without local reference
    - TestFormattingPreserved: comments between imports preserved after unused import removal
    - TestFormattingPreserved: indentation and blank line structure preserved
  </behavior>
  <action>
    Add test classes to tests/test_auto_fix.py:

    1. `TestUnusedImportsArbitraryNames`:
       - `test_removes_arbitrary_unused_import`: File with `from odoo import fields, api` where only `api.constrains` is used. Assert `fields` removed, `api` kept.
       - `test_removes_unknown_unused_import`: File with `from odoo import Command` and no usage of `Command` anywhere. Assert import line removed entirely.
       - `test_keeps_import_used_in_attribute_access`: File with `from odoo import fields` and `name = fields.Char(...)`. Assert `fields` is kept.
       - `test_removes_multiple_arbitrary_unused`: File with `from odoo.exceptions import ValidationError, UserError, AccessError` where only `ValidationError` is used. Assert `UserError` and `AccessError` removed, `ValidationError` kept.

    2. `TestUnusedImportsStarImport`:
       - `test_preserves_star_import`: File with `from odoo import *`. Assert import preserved (never remove star imports).

    3. `TestUnusedImportsAllExport`:
       - `test_preserves_import_in_all`: File with `from odoo import api` and `__all__ = ["api"]` but no other reference to `api`. Assert `api` import preserved.

    4. `TestFormattingPreserved`:
       - `test_preserves_comments_between_imports`: File with comment lines between import blocks. After removing unused import, comments remain intact.
       - `test_no_triple_blank_lines`: After removing an import, assert no 3+ consecutive blank lines appear.

    These tests should FAIL initially since the current whitelist only checks 4 names. Run to confirm RED.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && .venv/bin/python -m pytest tests/test_auto_fix.py -x -q -k "ArbitraryNames or StarImport or AllExport or FormattingPreserved" 2>&1 | tail -5</automated>
  </verify>
  <done>8 new test cases exist. ArbitraryNames tests FAIL (RED) confirming whitelist limitation. StarImport and Formatting tests may pass (existing behavior is conservative). All existing 67+ tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite fix_unused_imports with full AST body scan</name>
  <files>python/src/odoo_gen_utils/auto_fix.py</files>
  <action>
    **Part A: Add helper function `_find_all_name_references`** (insert near the existing import utility section, around line 1050):

    ```python
    def _find_all_name_references(tree: ast.Module, exclude_imports: bool = True) -> set[str]:
        """Collect every ast.Name.id in the module body, excluding import statements."""
        import_lines: set[int] = set()
        if exclude_imports:
            for node in ast.walk(tree):
                if isinstance(node, (ast.Import, ast.ImportFrom)):
                    # Exclude all lines the import spans (handles multi-line imports)
                    for line_no in range(node.lineno, (node.end_lineno or node.lineno) + 1):
                        import_lines.add(line_no)

        names: set[str] = set()
        for node in ast.walk(tree):
            if isinstance(node, ast.Name) and node.lineno not in import_lines:
                names.add(node.id)
        return names
    ```

    **Part B: Add helper `_find_all_in_module(tree)` to check __all__:**
    - Walk tree for ast.Assign where target is `__all__`.
    - If found, extract all string constants from the list value.
    - Return set of names that appear in __all__.

    **Part C: Rewrite `fix_unused_imports`:**
    - DELETE `_IMPORT_USAGE_PATTERNS` dict entirely (lines 1053-1058).
    - Rewrite `fix_unused_imports` to:
      1. Parse source with `ast.parse()`.
      2. Call `_find_all_name_references(tree)` to get all referenced names.
      3. Call `_find_all_in_module(tree)` to get __all__ exports (treat as used).
      4. Union referenced names with __all__ names = `used_names`.
      5. Walk ast.ImportFrom and ast.Import nodes:
         - Skip star imports (`alias.name == '*'`).
         - For each imported name (alias.asname or alias.name), check if in `used_names`.
         - Track names_to_keep vs names_to_remove per import node.
      6. For import nodes with removals:
         - If all names removed: remove entire import line(s).
         - If some names removed: rebuild import statement with only kept names (preserve leading indentation).
      7. Process import nodes in reverse line order (descending lineno) to avoid line-shift issues.
      8. Clean up consecutive blank lines.
      9. Write result if changes made.
    - Keep same function signature: `(file_path: Path) -> bool`.
    - Handle edge cases:
      - Empty file: return False (existing behavior).
      - SyntaxError: return False (existing behavior).
      - `from X import *`: skip entirely (never remove star imports).
      - Multi-line imports (`from X import (\n    a,\n    b,\n)`): handle end_lineno for full span removal.

    **Part D: Preserve attribute chain detection:**
    - `ast.Name` correctly handles attribute access: `api.constrains` has an `ast.Attribute` node with `value=ast.Name(id='api')`. So `api` appears as an `ast.Name` reference. No special handling needed -- the standard walk catches it.

    **Anti-patterns to avoid:**
    - Do NOT fall back to string search for any name. Use ONLY ast.Name references.
    - Do NOT keep `_IMPORT_USAGE_PATTERNS` as a fallback. Delete it entirely.
    - Do NOT use ast.unparse() for rebuilding import lines with reduced names. Use string formatting with the original indentation.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && .venv/bin/python -m pytest tests/test_auto_fix.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>fix_unused_imports uses full AST body scan via _find_all_name_references. _IMPORT_USAGE_PATTERNS dict is deleted. All tests pass including new ArbitraryNames/StarImport/AllExport/FormattingPreserved tests. All 75+ tests green. No new dependencies.</done>
</task>

</tasks>

<verification>
1. All tests pass: `cd python && .venv/bin/python -m pytest tests/test_auto_fix.py -x -q`
2. Arbitrary name tests pass: `cd python && .venv/bin/python -m pytest tests/test_auto_fix.py -x -q -k "ArbitraryNames"`
3. Star import preservation: `cd python && .venv/bin/python -m pytest tests/test_auto_fix.py -x -q -k "StarImport"`
4. Integration tests pass: `cd python && .venv/bin/python -m pytest tests/test_auto_fix_integration.py -x -q`
5. Full suite: `cd python && .venv/bin/python -m pytest tests/ -x -q`
6. Verify _IMPORT_USAGE_PATTERNS is deleted: `grep -n "_IMPORT_USAGE_PATTERNS" python/src/odoo_gen_utils/auto_fix.py` should return nothing
</verification>

<success_criteria>
- `_IMPORT_USAGE_PATTERNS` dict no longer exists in auto_fix.py
- `_find_all_name_references` function exists and uses ast.walk() + ast.Name
- fix_unused_imports detects arbitrary unused imports (not just 4 hardcoded names)
- Star imports are never removed
- Names in __all__ are treated as used
- Attribute chains (api.constrains) correctly count base name as used
- All 75+ tests pass (67 from Plan 01 + 8 new)
- 7 integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-auto-fix-ast-migration/20-02-SUMMARY.md`
</output>
