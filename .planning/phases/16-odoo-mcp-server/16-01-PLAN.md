---
phase: 16-odoo-mcp-server
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - python/pyproject.toml
  - python/src/odoo_gen_utils/mcp/__init__.py
  - python/src/odoo_gen_utils/mcp/odoo_client.py
  - python/src/odoo_gen_utils/mcp/server.py
  - python/tests/test_mcp_server.py
autonomous: true
requirements: [MCP-02]

must_haves:
  truths:
    - "MCP server exposes exactly 6 tools: check_connection, list_models, get_model_fields, list_installed_modules, check_module_dependency, get_view_arch"
    - "Each tool returns structured, human-readable string output (not raw XML-RPC dicts)"
    - "Tools return an ERROR-prefixed string when Odoo is unreachable (never crash the server)"
    - "OdooClient reads credentials from environment variables ODOO_URL, ODOO_DB, ODOO_USER, ODOO_API_KEY"
    - "Authentication is lazy (happens on first tool call, not server startup)"
    - "Unit tests verify all 6 tools with mocked XML-RPC responses"
  artifacts:
    - path: "python/src/odoo_gen_utils/mcp/__init__.py"
      provides: "Package init for mcp subpackage"
    - path: "python/src/odoo_gen_utils/mcp/odoo_client.py"
      provides: "OdooClient wrapper class with authenticate, execute_kw, search_read"
      exports: ["OdooConfig", "OdooClient"]
      min_lines: 50
    - path: "python/src/odoo_gen_utils/mcp/server.py"
      provides: "FastMCP server with 6 tool registrations and __main__ entry point"
      exports: ["mcp", "main"]
      min_lines: 120
    - path: "python/tests/test_mcp_server.py"
      provides: "Unit tests for all 6 MCP tools with mocked XML-RPC"
      min_lines: 150
  key_links:
    - from: "python/src/odoo_gen_utils/mcp/server.py"
      to: "python/src/odoo_gen_utils/mcp/odoo_client.py"
      via: "_get_client() creates OdooClient from env vars"
      pattern: "_get_client.*OdooClient"
    - from: "python/tests/test_mcp_server.py"
      to: "python/src/odoo_gen_utils/mcp/server.py"
      via: "Patches _get_client to inject mock OdooClient"
      pattern: "patch.*_get_client"
    - from: "python/src/odoo_gen_utils/mcp/odoo_client.py"
      to: "xmlrpc.client.ServerProxy"
      via: "ServerProxy instances for /xmlrpc/2/common and /xmlrpc/2/object"
      pattern: "ServerProxy.*xmlrpc"
---

<objective>
Implement the Odoo MCP server with OdooClient XML-RPC wrapper and all 6 introspection tools, using TDD (tests first, then implementation).

Purpose: This is the core deliverable of Phase 16 -- a FastMCP server that code generation agents can query for live Odoo model schemas, field definitions, installed modules, and view architectures. The OdooClient wrapper separates XML-RPC concerns from MCP tool logic, creating a clean mock boundary for testing.

Output: Working MCP server module (`odoo_gen_utils.mcp`) with 6 tools, OdooClient wrapper, and comprehensive unit tests with mocked XML-RPC responses.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-odoo-mcp-server/16-RESEARCH.md

# Phase 15 built the Docker dev instance and verify script that established XML-RPC patterns
@.planning/phases/15-odoo-dev-instance/15-01-SUMMARY.md

# Existing pyproject.toml needs dependency additions
@python/pyproject.toml

# Existing verify script shows the XML-RPC patterns already proven in this project
@scripts/verify-odoo-dev.py

<interfaces>
<!-- From python/pyproject.toml: Current project structure -->
<!-- Build: hatchling, packages at src/odoo_gen_utils -->
<!-- Test: pytest>=8.0 in [project.optional-dependencies].test -->
<!-- Python: >=3.12,<3.13 -->

From scripts/verify-odoo-dev.py (proven XML-RPC patterns):
```python
# Authentication
common = xmlrpc.client.ServerProxy(f"{url}/xmlrpc/2/common")
uid = common.authenticate(db, username, password, {})

# Model queries
models = xmlrpc.client.ServerProxy(f"{url}/xmlrpc/2/object")
result = models.execute_kw(db, uid, password, "ir.model", "search_count", [[]])

# Module queries
installed = models.execute_kw(db, uid, password, "ir.module.module", "search_read",
    [[["state", "=", "installed"]]], {"fields": ["name"]})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MCP dependencies and create package skeleton</name>
  <files>python/pyproject.toml, python/src/odoo_gen_utils/mcp/__init__.py</files>
  <action>
1. Update `python/pyproject.toml`:
   - Add new optional dependency group `mcp = ["mcp>=1.9"]`
   - Add `pytest-asyncio>=0.23` to the `test` optional dependency group
   - Add `asyncio_mode = "auto"` to `[tool.pytest.ini_options]` section

2. Create `python/src/odoo_gen_utils/mcp/__init__.py`:
   - Empty init file with docstring: "Odoo MCP server for model introspection via XML-RPC."

3. Install new dependencies:
   - Run `cd python && uv pip install "mcp>=1.9" "pytest-asyncio>=0.23"` to install into the existing venv
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && python -c "import mcp; print(f'mcp {mcp.__version__}')" && python -c "import pytest_asyncio; print('pytest-asyncio OK')" && python -c "from odoo_gen_utils.mcp import __doc__; print(__doc__)"</automated>
  </verify>
  <done>pyproject.toml has mcp and pytest-asyncio dependencies, asyncio_mode=auto configured, mcp package is importable, odoo_gen_utils.mcp package exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD -- OdooClient wrapper + MCP server with 6 tools</name>
  <files>python/tests/test_mcp_server.py, python/src/odoo_gen_utils/mcp/odoo_client.py, python/src/odoo_gen_utils/mcp/server.py</files>
  <behavior>
    - test_odoo_config_from_env: OdooConfig reads ODOO_URL, ODOO_DB, ODOO_USER, ODOO_API_KEY from env vars with defaults (http://localhost:8069, odoo_dev, admin, admin)
    - test_odoo_client_authenticate: OdooClient.authenticate() calls common.authenticate and caches uid; raises ConnectionError if uid is falsy
    - test_odoo_client_search_read: OdooClient.search_read() calls execute_kw with correct args
    - test_list_tools: MCP server exposes exactly 6 tools by name
    - test_check_connection: Returns version string and uid when connected
    - test_list_models: Returns formatted list with model names and descriptions; supports name_filter
    - test_get_model_fields: Returns field name, ttype, relation, required, readonly for given model; handles empty result
    - test_list_installed_modules: Returns installed module names and versions
    - test_check_module_dependency_installed: Returns "INSTALLED" with version for installed module
    - test_check_module_dependency_not_found: Returns "not found" for unknown module
    - test_check_module_dependency_not_installed: Returns "NOT installed" with state for uninstalled module
    - test_get_view_arch: Returns XML architecture with view name and type; supports view_type filter
    - test_connection_error: All tools return "ERROR: Cannot connect" when Odoo is unreachable (ConnectionRefusedError)
    - test_xmlrpc_fault: All tools return "ERROR: Odoo XML-RPC fault" on xmlrpc.client.Fault
  </behavior>
  <action>
**RED phase:** Create `python/tests/test_mcp_server.py` with all tests listed in behavior. Use the in-memory MCP Client test pattern from the research:
- Create `mock_client` fixture returning a MagicMock OdooClient with canned responses
- Create `patched_get_client` fixture that patches `odoo_gen_utils.mcp.server._get_client`
- Create `mcp_client` fixture that instantiates `Client(transport=server)` from the mcp SDK
- Use `pytestmark = pytest.mark.asyncio(mode="auto")` for async test discovery

IMPORTANT: If `from mcp.client import Client` does not work, fall back to calling tool functions directly via the FastMCP instance's `call_tool()` method. Check the research open question #2.

Also create separate test class `TestOdooClient` for OdooClient unit tests (sync, no MCP client needed):
- Mock `xmlrpc.client.ServerProxy` directly
- Test authenticate success/failure
- Test search_read delegation
- Test OdooConfig env var reading

Run tests -- they should ALL FAIL (modules don't exist yet).

**GREEN phase:** Create implementation files:

1. `python/src/odoo_gen_utils/mcp/odoo_client.py`:
   - `OdooConfig` frozen dataclass: url, db, username, api_key
   - `OdooClient` class with `__init__`, `authenticate()`, `uid` property (lazy), `execute_kw()`, `search_read()`
   - Follow the exact pattern from 16-RESEARCH.md Pattern 1
   - Use `xmlrpc.client.ServerProxy` for `/xmlrpc/2/common` and `/xmlrpc/2/object`

2. `python/src/odoo_gen_utils/mcp/server.py`:
   - `logging.basicConfig(stream=sys.stderr)` -- CRITICAL: never use print() (corrupts stdio JSON-RPC)
   - `mcp = FastMCP("odoo-introspection")` server instance
   - `_client: OdooClient | None = None` module-level lazy singleton
   - `_get_client()` function reading env vars with defaults matching Phase 15 dev instance
   - `_handle_error(exc)` function for consistent error formatting
   - 6 `@mcp.tool()` decorated functions:
     a. `check_connection()` -- calls version() + authenticates, returns version string + uid
     b. `list_models(name_filter="", limit=100)` -- queries ir.model, formats as bullet list
     c. `get_model_fields(model_name)` -- queries ir.model.fields with name, ttype, relation, required, readonly, field_description
     d. `list_installed_modules()` -- queries ir.module.module where state=installed
     e. `check_module_dependency(module_name)` -- checks if specific module is installed
     f. `get_view_arch(model_name, view_type="")` -- queries ir.ui.view with arch field
   - Each tool wraps its body in try/except, returns error string via _handle_error on failure
   - `main()` function calling `mcp.run(transport="stdio")`
   - `if __name__ == "__main__": main()` block
   - Add `__main__.py` support: create `python/src/odoo_gen_utils/mcp/__main__.py` with `from .server import main; main()`

Run tests -- they should ALL PASS.

**REFACTOR phase:** Review for clarity, ensure no print() calls, verify all tools follow identical error handling pattern, check docstrings provide clear descriptions for LLM consumption.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && python -m pytest tests/test_mcp_server.py -x -v</automated>
  </verify>
  <done>All unit tests pass. OdooClient wraps XML-RPC with authenticate/search_read/execute_kw. MCP server exposes 6 tools via @mcp.tool() decorators. Error handling returns string errors (never crashes). No print() calls in server code. Tests use mocked XML-RPC (no Docker required).</done>
</task>

</tasks>

<verification>
1. `cd /home/inshal-rauf/Odoo_module_automation/python && python -m pytest tests/test_mcp_server.py -x -v` -- all MCP tests pass
2. `cd /home/inshal-rauf/Odoo_module_automation/python && python -m pytest tests/ -x -q` -- full test suite passes (no regressions)
3. `cd /home/inshal-rauf/Odoo_module_automation/python && python -c "from odoo_gen_utils.mcp.server import mcp; print(mcp.name)"` -- prints "odoo-introspection"
4. `cd /home/inshal-rauf/Odoo_module_automation/python && grep -c 'print(' src/odoo_gen_utils/mcp/server.py` -- returns 0 (no print calls)
</verification>

<success_criteria>
- MCP server module exists at `python/src/odoo_gen_utils/mcp/server.py` with 6 @mcp.tool() registered functions
- OdooClient wrapper at `python/src/odoo_gen_utils/mcp/odoo_client.py` with authenticate/execute_kw/search_read
- All unit tests pass with mocked XML-RPC (no Docker dependency)
- Full existing test suite passes without regressions
- No print() calls in server module (logging to stderr only)
- Each tool handles ConnectionRefusedError, OSError, xmlrpc.client.Fault gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/16-odoo-mcp-server/16-01-SUMMARY.md`
</output>
