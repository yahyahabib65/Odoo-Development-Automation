---
phase: 28-computed-chains-cycle-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/src/odoo_gen_utils/renderer.py
  - python/tests/test_renderer.py
  - python/tests/test_render_stages.py
autonomous: true
requirements: [SPEC-03, SPEC-05]

must_haves:
  truths:
    - "A spec with computation_chains generates @api.depends with correct dotted paths and store=True"
    - "Computed fields within a model are topologically sorted so downstream fields follow upstream"
    - "A spec with circular dependency chains is rejected with actionable error before any files are generated"
    - "Specs without computation_chains continue to work unchanged (backward compat)"
  artifacts:
    - path: "python/src/odoo_gen_utils/renderer.py"
      provides: "_validate_no_cycles(), _process_computation_chains(), _topologically_sort_fields(), _resolve_comodel()"
      contains: "graphlib"
    - path: "python/tests/test_renderer.py"
      provides: "TestProcessComputationChains, TestValidateNoCycles, TestTopologicallySortFields"
    - path: "python/tests/test_render_stages.py"
      provides: "TestRenderModelsComputedChains"
  key_links:
    - from: "renderer.py::render_module()"
      to: "_validate_no_cycles()"
      via: "Called FIRST in render_module(), before _process_relationships()"
      pattern: "_validate_no_cycles\\(spec\\)"
    - from: "renderer.py::render_module()"
      to: "_process_computation_chains()"
      via: "Called AFTER _process_relationships(), before rendering"
      pattern: "_process_computation_chains\\(spec\\)"
    - from: "renderer.py::_build_model_context()"
      to: "_topologically_sort_fields()"
      via: "Sorts computed_fields list before passing to template"
      pattern: "_topologically_sort_fields\\(computed_fields\\)"
---

<objective>
Add computation chain preprocessing and cycle detection to the Odoo module renderer.

Purpose: Enable multi-model computed field dependency chains (e.g., sale.order.amount_total depends on sale.order.line.price_subtotal) with correct @api.depends dotted paths, store=True injection, topological field ordering, and early rejection of circular dependencies.

Output: Updated renderer.py with 4 new functions, wired into render_module() and _build_model_context(). All existing tests pass, new tests cover chain processing, cycle detection, and topological sorting.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-computed-chains-cycle-detection/28-RESEARCH.md
@.planning/phases/28-computed-chains-cycle-detection/28-VALIDATION.md
@.planning/phases/27-relationship-patterns/27-01-SUMMARY.md

@python/src/odoo_gen_utils/renderer.py
@python/tests/test_renderer.py
@python/tests/test_render_stages.py

<interfaces>
<!-- Key functions and patterns the executor needs from renderer.py -->

From renderer.py (current pipeline):
```python
def render_module(spec, template_dir, output_dir, verifier=None):
    env = create_versioned_renderer(spec.get("odoo_version", "17.0"))
    spec = _process_relationships(spec)  # Phase 27
    module_name = spec["module_name"]
    # ... stages ...

def _process_relationships(spec: dict[str, Any]) -> dict[str, Any]:
    # Pure function, returns new spec with enriched models
    ...

def _build_model_context(spec: dict[str, Any], model: dict[str, Any]) -> dict[str, Any]:
    # Line 393: computed_fields = [f for f in fields if f.get("compute")]
    # Line 517: "computed_fields": computed_fields,
    ...
```

From model.py.j2 template (line 144-151):
```jinja2
{% for field in computed_fields %}
    @api.depends({% for dep in field.depends %}"{{ dep }}"{% if not loop.last %}, {% endif %}{% endfor %})
    def {{ field.compute }}(self):
        ...
{% endfor %}
```

Field store rendering (line 90-91):
```jinja2
{% if field.store is defined and field.store %}
        store=True,
{% endif %}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cycle detection, chain preprocessor, and topological sort functions</name>
  <files>python/src/odoo_gen_utils/renderer.py, python/tests/test_renderer.py</files>
  <behavior>
    - TestValidateNoCycles::test_valid_chains_pass: spec with valid A->B chain passes without error
    - TestValidateNoCycles::test_circular_raises: spec with A->B->A chain raises ValueError
    - TestValidateNoCycles::test_error_names_participants: ValueError message contains cycle field names
    - TestValidateNoCycles::test_cross_model_cycle: cycle spanning 2 models detected via comodel resolution
    - TestValidateNoCycles::test_no_chains_passthrough: spec without computation_chains passes silently
    - TestProcessComputationChains::test_enriches_depends: chain entry sets field.depends to depends_on list
    - TestProcessComputationChains::test_sets_store_true: chain fields get store=True
    - TestProcessComputationChains::test_injects_compute_name: field without compute= gets _compute_{name}
    - TestProcessComputationChains::test_dotted_paths_preserved: "enrollment_ids.weighted_grade" preserved in depends
    - TestProcessComputationChains::test_no_chains_passthrough: spec without computation_chains returned unchanged
    - TestProcessComputationChains::test_does_not_mutate_input: original spec dict is not mutated
    - TestTopologicallySortFields::test_sort_order: field depending on another computed field comes after it
    - TestTopologicallySortFields::test_independent_preserves_order: fields with no inter-deps keep original order
    - TestTopologicallySortFields::test_single_field: single computed field returned as-is
  </behavior>
  <action>
    **RED phase:** Add unit tests to test_renderer.py in three new test classes:
    - `TestValidateNoCycles` (5 tests) -- use university enrollment/student spec from RESEARCH.md examples
    - `TestProcessComputationChains` (6 tests) -- verify enrichment, immutability, passthrough
    - `TestTopologicallySortFields` (3 tests) -- verify ordering within a model

    Import the new functions: `from odoo_gen_utils.renderer import _validate_no_cycles, _process_computation_chains, _topologically_sort_fields`

    Run tests -- all 14 must FAIL (ImportError since functions don't exist yet).

    **GREEN phase:** Implement in renderer.py (add near top, after `_process_relationships()`):

    1. `_resolve_comodel(spec, model_name, field_name) -> str | None` -- looks up comodel_name of a relational field
    2. `_validate_no_cycles(spec) -> None` -- builds dependency graph from `computation_chains` (resolving dotted paths via _resolve_comodel), uses `graphlib.TopologicalSorter` to detect cycles, raises `ValueError` with cycle participant names on `CycleError`. Also include inline computed fields that have `depends` referencing other computed fields (per Open Question 3 in RESEARCH.md). Early return if no `computation_chains` AND no inline computed cross-deps.
    3. `_process_computation_chains(spec) -> dict[str, Any]` -- pure function, returns new spec. For each chain entry: parse `field` as `model_name.field_name` via rsplit(".", 1), find matching field in matching model, create new field dict with `depends=chain["depends_on"]`, `store=True`, `compute=field.get("compute", f"_compute_{field_name}")`. Fields/models not in chains pass through unchanged.
    4. `_topologically_sort_fields(computed_fields: list[dict]) -> list[dict]` -- takes list of computed field dicts, builds graph of local (non-dotted) deps that are also in the computed set, returns topologically sorted list. Falls back to original order on CycleError (already caught by _validate_no_cycles).

    Wire into pipeline:
    - In `render_module()`: add `_validate_no_cycles(spec)` as FIRST line (before `env = create_versioned_renderer(...)`), add `spec = _process_computation_chains(spec)` after `spec = _process_relationships(spec)`
    - In `_build_model_context()`: after line 393 (`computed_fields = [f for f in fields if f.get("compute")]`), add: `if len(computed_fields) > 1: computed_fields = _topologically_sort_fields(computed_fields)`

    Add `from graphlib import TopologicalSorter, CycleError` to imports at top of renderer.py.

    Run tests -- all 14 must PASS.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && .venv/bin/python -m pytest tests/test_renderer.py -x -q -k "NoCycles or ComputationChains or TopologicallySortFields"</automated>
  </verify>
  <done>14 new unit tests pass. _validate_no_cycles rejects circular specs with actionable errors. _process_computation_chains enriches fields with depends/store/compute. _topologically_sort_fields orders computed fields by dependency. No mutation of input specs. No regressions in existing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for end-to-end chain rendering and cycle rejection</name>
  <files>python/tests/test_render_stages.py, python/src/odoo_gen_utils/renderer.py</files>
  <behavior>
    - TestRenderModelsComputedChains::test_cross_model_depends: render_models() with computation_chains spec produces model.py containing @api.depends("enrollment_ids.weighted_grade") and store=True on the field definition
    - TestRenderModelsComputedChains::test_chain_field_has_compute_method: generated model.py contains def _compute_gpa(self) method stub
    - TestRenderModelsComputedChains::test_topological_order_in_output: in generated model.py for a model with 2 intra-model chain fields, upstream field's compute method appears before downstream field's
    - TestRenderModelsComputedChains::test_no_files_on_cycle: render_module() with circular chain raises ValueError; output directory contains no generated files
    - TestRenderModelsComputedChains::test_backward_compat_no_chains: render_module() with spec that has no computation_chains produces identical output to before (no regressions)
  </behavior>
  <action>
    **RED phase:** Add `TestRenderModelsComputedChains` class to test_render_stages.py with 5 integration tests. Use the university enrollment/student spec from RESEARCH.md. For the cycle test, create a spec where student.gpa depends on enrollment.weighted_grade and enrollment.weighted_grade depends on student.gpa. For backward compat test, use an existing spec fixture that has no computation_chains.

    For test_cross_model_depends: call `render_models()` (or `render_module()`), read the generated student model.py, assert it contains `@api.depends("enrollment_ids.weighted_grade"` and `store=True`.

    For test_no_files_on_cycle: use `pytest.raises(ValueError, match="Circular dependency")`, then assert output_dir is empty or does not exist.

    Run tests -- all 5 must FAIL.

    **GREEN phase:** If any test failures are due to implementation bugs (not just RED phase), fix in renderer.py. The implementation from Task 1 should handle most cases. Likely adjustments:
    - Ensure _build_model_context() re-extracts computed_fields AFTER the field list has been enriched by _process_computation_chains() (the enrichment adds `compute=` to fields that only had it in chains, so the `computed_fields` filter at line 393 must pick them up)
    - Ensure the topological sort handles the case where a field's `depends` list contains dotted paths (these should be ignored for intra-model sorting since they are cross-model)

    Run tests -- all 5 must PASS. Then run full suite to confirm zero regressions.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation/python && .venv/bin/python -m pytest tests/test_render_stages.py -x -q -k "ComputedChains" && .venv/bin/python -m pytest tests/ -x -q</automated>
  </verify>
  <done>5 integration tests pass. Generated model.py files contain correct @api.depends with dotted paths, store=True, and compute method stubs. Circular specs rejected before file creation. Full test suite green with zero regressions.</done>
</task>

</tasks>

<verification>
1. Unit tests: `cd python && .venv/bin/python -m pytest tests/test_renderer.py -x -q -k "NoCycles or ComputationChains or TopologicallySortFields"` -- 14 tests pass
2. Integration tests: `cd python && .venv/bin/python -m pytest tests/test_render_stages.py -x -q -k "ComputedChains"` -- 5 tests pass
3. Full regression: `cd python && .venv/bin/python -m pytest tests/ -x -q` -- all tests pass, zero regressions
4. Cycle detection fires before file generation (no partial output on invalid spec)
</verification>

<success_criteria>
- computation_chains spec section enriches fields with depends, store=True, and compute method name
- Circular dependencies rejected with ValueError naming cycle participants
- Computed fields topologically sorted within each model
- No mutation of input spec dicts (immutability preserved)
- Backward compatibility: specs without computation_chains work identically to before
- 19 new tests (14 unit + 5 integration), full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/28-computed-chains-cycle-detection/28-01-SUMMARY.md`
</output>
