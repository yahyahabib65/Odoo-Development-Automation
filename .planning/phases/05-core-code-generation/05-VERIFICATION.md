---
phase: 05-core-code-generation
verified: 2026-03-02T00:00:00Z
status: verified
score: 20/20 must-haves verified
re_verification: "2026-03-02T17:00:00Z"
gaps:
  - truth: "REQUIREMENTS.md traceability table marks CODG-04, CODG-05, CODG-06, CODG-07 as Pending despite full implementation"
    status: resolved
    reason: "Plan 05-01 claimed CODG-05, CODG-06, CODG-07 and the renderer generates action/menu XML (CODG-04), __init__.py files (CODG-05), data/sequences files (CODG-06), and wizard files (CODG-07). The requirements body text has [ ] (unchecked) and the traceability table says 'Pending' for all four. Only the declarative implementation is present — the REQUIREMENTS.md completion status was never updated."
    artifacts:
      - path: ".planning/REQUIREMENTS.md"
        issue: "CODG-04, CODG-05, CODG-06, CODG-07 marked Pending/unchecked despite implementation existing"
    missing:
      - "Mark CODG-05, CODG-06, CODG-07 as [x] Complete in REQUIREMENTS.md (claimed by plan 05-01)"
      - "Mark CODG-04 as [x] Complete in REQUIREMENTS.md (action/menu XML generated by renderer, not claimed by any plan but demonstrably implemented)"
      - "Update traceability table rows for CODG-04..07 from 'Pending' to 'Complete'"
human_verification:
  - test: "Trigger /odoo-gen:plan with a sample description and verify the spec approval flow calls generate.md"
    expected: "After approving a spec, the system runs render-module CLI, then odoo-model-gen for models with computed fields, then odoo-view-gen + odoo-test-gen in parallel"
    why_human: "Cannot verify the @workflows/generate.md execution chain programmatically — requires interactive AI coding assistant session"
  - test: "Run odoo-model-gen on a generated model file with TODO stubs"
    expected: "All # TODO stubs replaced with real OCA-compliant method bodies; for rec in self: used in computed/constrained methods; self.field used in onchange"
    why_human: "Agent execution requires live Claude Code session — cannot verify prompt-following behavior from file content alone"
---

# Phase 5: Core Code Generation Verification Report

**Phase Goal:** System generates complete, real Odoo module code (not stubs) from an approved specification, following OCA standards.
**Verified:** 2026-03-02
**Status:** gaps_found (1 documentation gap; all implementation verified)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | render-module CLI produces computed field declarations with @api.depends stub methods from a spec with compute/depends fields | VERIFIED | `model.py.j2` emits `@api.depends` stub with `# TODO` body; functional test confirms `@api.depends("qty", "price")` in rendered output |
| 2 | render-module CLI produces sequences.xml and _default_<field>() method when a Char field named reference/ref/number/code/sequence is required | VERIFIED | `sequences.xml.j2` with `<odoo noupdate="1">` and `ir.sequence` records; `_default_reference` method in model; test `test_sequence_field_generates_sequences_xml` passes |
| 3 | render-module CLI produces wizard.py and wizard_form.xml files when spec.wizards array is non-empty | VERIFIED | `wizard.py.j2` (TransientModel + default_get + button_confirm) and `wizard_form.xml.j2` (footer with special="cancel") generated; `wizards/__init__.py` created; tests pass |
| 4 | Form views include header with statusbar widget when model has a state or status Selection field | VERIFIED | `view_form.xml.j2` line 11-25 emits `<header>` with `widget="statusbar"` and `statusbar_visible` when `state_field` is set; functional render confirms |
| 5 | Wizard buttons appear in parent model form header with invisible expression | VERIFIED | `view_form.xml.j2` lines 14-22 emit `<button invisible="{{ state_field.name }} != '{{ wizard.trigger_state }}'"` for each matching wizard; functional test confirms `action_open_test_wizard` in rendered view |
| 6 | Manifest data list follows canonical load order: security -> sequences -> data -> views -> actions -> menu -> wizard views | VERIFIED | `_compute_manifest_data()` builds list in exact canonical order; functional render shows `security.xml -> ir.model.access.csv -> sequences.xml -> data.xml -> views -> menu.xml -> wizard_form.xml` |
| 7 | __init__.py root imports wizards subpackage when wizards exist | VERIFIED | `init_root.py.j2` line 3-4: `{% if has_wizards %}from . import wizards{% endif %}`; functional render shows `from . import wizards` in root `__init__.py` |
| 8 | odoo-model-gen agent can rewrite a Jinja2-rendered model file with complete, OCA-compliant computed field bodies (no # TODO stubs remain) | VERIFIED | `agents/odoo-model-gen.md` contains full system prompt with 6-step execution (Read model → Read spec → Identify TODOs → Infer → Write → Confirm); Write tool to same path documented |
| 9 | odoo-model-gen agent produces correct @api.depends, @api.onchange, @api.constrains method bodies that iterate 'for rec in self:' for computed/constrained methods | VERIFIED | REQUIRED patterns section in `odoo-model-gen.md` explicitly documents `for rec in self:` for computed/constrained and `self.field =` for onchange; code example shows both patterns |
| 10 | odoo-view-gen agent enriches form views with action buttons for each workflow state transition | VERIFIED | `agents/odoo-view-gen.md` contains Wave 2 enrichment prompt with complete `<header>` example showing draft/confirmed/done buttons; full XML example included |
| 11 | odoo-test-gen agent generates TransactionCase tests for computed fields (set deps, assert result) | VERIFIED | `agents/odoo-test-gen.md` has Phase 5 scope with `TransactionCase`, `setUpClass`, `test_compute_{field}_basic` + `test_compute_{field}_zero_case` templates |
| 12 | All three agents have FORBIDDEN list preventing Odoo 16 anti-patterns (attrs=, states=, @api.multi, list tag, etc.) | VERIFIED | model-gen: 8-item FORBIDDEN list; view-gen: 4-item FORBIDDEN XML list (`attrs=`, `states=`, `<list>`, `statusbar` in sheet); test-gen: `SavepointCase` FORBIDDEN |
| 13 | User who approves a spec in spec.md sees a call to generate.md at the end of the approval step | VERIFIED | `spec.md` Step 4.3 lines 457-464: Step 3 is "Trigger code generation — Execute the generate.md workflow" with `@workflows/generate.md` reference, placed before report step |
| 14 | generate.md orchestrates two waves: Wave 1 (render-module CLI + odoo-model-gen), Wave 2 (odoo-view-gen + odoo-test-gen) | VERIFIED | `workflows/generate.md` has 5 steps: Step 1 render-module, Step 2 Wave 1 model method bodies, Step 3 Wave 2 parallel view/test, Step 4 commit, Step 5 report |
| 15 | generate.md Wave 1 completes fully before Wave 2 spawns (sequential, not concurrent) | VERIFIED | `generate.md` Step 2 line 66-82: "IMPORTANT: Wave 1 must complete fully before Wave 2 begins" and "Wait for ALL odoo-model-gen tasks to complete before proceeding to Wave 2" |
| 16 | generate.md includes a git commit step after all generation completes | VERIFIED | `generate.md` Step 4 lines 116-129: `git add "$OUTPUT_DIR/$MODULE_NAME/" && git commit -m "feat($MODULE_NAME): generate complete Odoo 17.0 module from spec"` |
| 17 | REQUIREMENTS.md CODG-09 wording corrects list to tree for Odoo 17.0 | VERIFIED | `REQUIREMENTS.md` line: "e.g., `<tree>` not `<list>` — `<list>` is Odoo 18+ only" |
| 18 | REQUIREMENTS.md CODG-10 wording notes README.rst (OCA standard) not .md | VERIFIED | `REQUIREMENTS.md` line: "System generates a README.rst... (OCA standard is .rst, not .md)" |
| 19 | REQUIREMENTS.md CODG-02 has a comment noting CRUD overrides deferred to Phase 7 per Decision A | VERIFIED | `REQUIREMENTS.md` has HTML comment after CODG-02: "CRUD overrides (create/write/unlink) deferred to Phase 7 per CONTEXT.md Decision A" |
| 20 | REQUIREMENTS.md traceability table updated to mark CODG-04..07 Complete | FAILED | CODG-04, CODG-05, CODG-06, CODG-07 all show `[ ]` (unchecked) and "Pending" in traceability table — implementation exists but completion not recorded |

**Score:** 19/20 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|---------|--------|---------|
| `python/src/odoo_gen_utils/renderer.py` | `_build_model_context` with 9 new Phase 5 context keys; `render_module` generating wizard/sequence/data files | VERIFIED | All 9 keys present: `computed_fields`, `onchange_fields`, `constrained_fields`, `sequence_fields`, `sequence_field_names`, `state_field`, `wizards`, `has_computed`, `has_sequence_fields`; render_module generates canonical manifest ordering |
| `python/src/odoo_gen_utils/templates/model.py.j2` | Computed field stubs with @api.depends; sequence field declarations with readonly/copy=False; method stubs for onchange/constrains | VERIFIED | 123 lines; sequence Char branch at line 48; computed @api.depends at line 100-107; onchange at 108-113; constrains at 114-122; ValidationError import conditional on constrained_fields |
| `python/src/odoo_gen_utils/templates/view_form.xml.j2` | `<header>` with statusbar widget when state field; wizard buttons with invisible expression | VERIFIED | 166 lines; `<header>` block at lines 11-26 conditional on `state_field`; statusbar_visible with `map(attribute=0)` join; wizard buttons with invisible expression |
| `python/src/odoo_gen_utils/templates/wizard.py.j2` | TransientModel wizard Python file | VERIFIED | 49 lines; `TransientModel`; `default_get` with `active_id` context injection; `button_confirm` with `ensure_one()` and `{'type': 'ir.actions.act_window_close'}` return |
| `python/src/odoo_gen_utils/templates/wizard_form.xml.j2` | Wizard dialog form XML with `<footer>` (not `<header>`) for buttons; `special="cancel"`; `target="new"` | VERIFIED | 35 lines; `<footer>` for buttons; `special="cancel"` on cancel button; `target="new"` on action record |
| `python/src/odoo_gen_utils/templates/sequences.xml.j2` | `<odoo noupdate="1">` with `ir.sequence` records | VERIFIED | 18 lines; `<odoo noupdate="1">` root; `ir.sequence` model; code, prefix, padding, number_next, number_increment fields |
| `python/src/odoo_gen_utils/templates/init_wizards.py.j2` | `__init__.py` for wizards/ subdirectory with `from . import {wizard_var}` per wizard | VERIFIED | 5 lines; loops over `spec_wizards` and emits `from . import {{ wizard.name | to_python_var }}` |
| `python/src/odoo_gen_utils/templates/init_root.py.j2` | Conditionally imports wizards subpackage when `has_wizards` | VERIFIED | 6 lines; `{% if has_wizards %}from . import wizards{% endif %}` |
| `agents/odoo-model-gen.md` | Full system prompt with FORBIDDEN list, REQUIRED patterns, execution steps, KB @includes; no "not yet available" | VERIFIED | 154 lines; 8-item FORBIDDEN list; 3 REQUIRED patterns with code examples; 6-step execution; KB @includes MASTER.md + models.md + inheritance.md; wizard action method pattern; no "not yet available" |
| `agents/odoo-view-gen.md` | Full view enrichment prompt with FORBIDDEN XML list, state button patterns, KB @includes; no "not yet available" | VERIFIED | 78 lines; 4-item FORBIDDEN XML list; complete `<header>` example with state buttons; REQUIRED patterns; KB @includes MASTER.md + views.md + actions.md; no "not yet available" |
| `agents/odoo-test-gen.md` | Phase 5 scope prompt with TransactionCase patterns, computed field test template, KB @includes | VERIFIED | 81 lines; Phase 5 scope declared; TransactionCase patterns; `test_compute_{field}_basic` + `test_compute_{field}_zero_case` templates; `assertRaises(ValidationError)` for constraints; KB @includes MASTER.md + testing.md |
| `workflows/generate.md` | Two-wave generation orchestration with 5 steps; Wave 1 sequential guard; Wave 2 parallel; git commit | VERIFIED | 162 lines; 5 steps (render-module → Wave 1 → Wave 2 → commit → report); sequential guard documented in Step 2; Task tool spawn prompts for each agent; error handling section |
| `workflows/spec.md` | generate.md trigger in Step 4.3 approval path | VERIFIED | Step 4.3 lines 457-464: "Trigger code generation — Execute the generate.md workflow" as Step 3 |
| `.planning/REQUIREMENTS.md` | CODG-09 corrected (tree not list); CODG-10 corrected (rst not md); CODG-02 deferral note | VERIFIED (partial) | CODG-09 and CODG-10 wording corrected; CODG-02 has deferral comment; but CODG-04/05/06/07 status not updated |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `renderer.py _build_model_context()` | `model.py.j2` | `computed_fields`, `onchange_fields`, `constrained_fields`, `sequence_fields`, `state_field` template vars | WIRED | All 9 new context keys confirmed in `_build_model_context()` return dict (lines 183-193); template uses them in loops at lines 94-122 |
| `renderer.py render_module()` | `wizard.py.j2` + `wizard_form.xml.j2` | wizards list from spec | WIRED | `render_module()` lines 431-460: `render_template(env, "wizard.py.j2", ...)` and `render_template(env, "wizard_form.xml.j2", ...)` per wizard in `spec_wizards` |
| `renderer.py render_module()` | `sequences.xml.j2` | `sequence_fields` detected per model | WIRED | Lines 390-415: `if has_sequences` guard; `sequences_ctx` built with `sequence_models`; `render_template(env, "sequences.xml.j2", ...)` |
| `workflows/spec.md Step 4.3` | `workflows/generate.md` | explicit `@workflows/generate.md` call after spec commit | WIRED | Lines 457-464 in `spec.md` explicitly reference and execute `generate.md` workflow |
| `workflows/generate.md Step 1` | `render-module CLI` | `odoo-gen-utils render-module --spec-file` | WIRED | `generate.md` lines 41-44 contain the CLI command with `--spec-file "$SPEC_PATH"` and `--output-dir "$OUTPUT_DIR"` |
| `workflows/generate.md Wave 1` | `odoo-model-gen agent` | Task tool spawn | WIRED | `generate.md` Step 2 lines 72-84: Task prompt for `odoo-model-gen` per model with TODO stubs |
| `workflows/generate.md Wave 2` | `odoo-view-gen + odoo-test-gen` | Task tool spawn (parallel) | WIRED | `generate.md` Step 3 lines 88-113: Task A (odoo-view-gen) and Task B (odoo-test-gen) spawn prompts |
| `agents/odoo-model-gen.md` | `~/.claude/odoo-gen/knowledge/models.md` | KB @include directive | WIRED | Line 96: `@~/.claude/odoo-gen/knowledge/models.md` |
| `agents/odoo-view-gen.md` | `~/.claude/odoo-gen/knowledge/views.md` | KB @include directive | WIRED | Line 72: `@~/.claude/odoo-gen/knowledge/views.md` |
| `agents/odoo-test-gen.md` | `~/.claude/odoo-gen/knowledge/testing.md` | KB @include directive | WIRED | Line 76: `@~/.claude/odoo-gen/knowledge/testing.md` |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| CODG-01 | 05-01, 05-03 | Complete `__manifest__.py` with correct version, dependencies, data file references | SATISFIED | `_compute_manifest_data()` produces canonical ordering; functional render confirms manifest with all 8 data file entries in correct order |
| CODG-02 | 05-01, 05-02 | Python model files with fields, computed fields, onchange, constraints (CRUD deferred to Phase 7) | SATISFIED (partial per Decision A) | `model.py.j2` generates field declarations, @api.depends, @api.onchange, @api.constrains stubs; `odoo-model-gen` fills method bodies; CRUD deferred per CONTEXT.md Decision A |
| CODG-03 | 05-01, 05-02 | XML view files (form, list, search views) | SATISFIED | `view_form.xml.j2` renders form + tree + search views; form includes `<header>` when state field present; no `attrs=` or `states=` |
| CODG-04 | (not claimed by any Phase 5 plan) | Action and menu XML files | SATISFIED (implementation) / NOT UPDATED (status) | `render_module()` generates `views/{model_var}_action.xml` (line 356-360) and `views/menu.xml` (line 364); REQUIREMENTS.md traceability shows "Pending" — status not updated |
| CODG-05 | 05-01 | `__init__.py` files with correct import chains | SATISFIED (implementation) / NOT UPDATED (status) | `render_module()` generates `__init__.py`, `models/__init__.py`, `tests/__init__.py`, `wizards/__init__.py`; `init_root.py.j2` conditionally imports wizards; REQUIREMENTS.md shows "Pending" |
| CODG-06 | 05-01 | Data files (sequences, default configuration) | SATISFIED (implementation) / NOT UPDATED (status) | `sequences.xml.j2` with `noupdate="1"` generated when sequence fields exist; `data.xml` always generated; REQUIREMENTS.md shows "Pending" |
| CODG-07 | 05-01 | Wizard (TransientModel) files | SATISFIED (implementation) / NOT UPDATED (status) | `wizard.py.j2` and `wizard_form.xml.j2` generated per wizard; `wizards/__init__.py` created; REQUIREMENTS.md shows "Pending" |
| CODG-08 | 05-01, 05-02 | OCA coding standards (PEP 8, 120 char line length, proper import ordering) | SATISFIED | Templates use OCA import ordering (`from odoo import api, fields, models`); FORBIDDEN list in agents prevents old API patterns; `ruff` configured in `pyproject.toml` |
| CODG-09 | 05-01, 05-03 | Correct Odoo 17.0 XML syntax (`<tree>` not `<list>`, inline `invisible`/`readonly`) | SATISFIED | No `<list>` tag in any template; no `attrs=` in any template; `view_form.xml.j2` uses `readonly="{{ state_field.name }} != 'draft'"` inline; REQUIREMENTS.md corrected |
| CODG-10 | 05-03 | README.rst (OCA standard) | SATISFIED | `readme.rst.j2` template exists; `render_module()` line 494-496 generates `README.rst`; REQUIREMENTS.md corrected to say `.rst` |

**Orphaned requirements (assigned to Phase 5 but not claimed by any plan):**
- CODG-04: Not claimed by 05-01, 05-02, or 05-03 frontmatter, yet implemented by existing `render_module()` action/menu rendering inherited from pre-Phase-5 code. Status should be updated to Complete.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `python/tests/test_renderer.py` | 13 | `from python.src.odoo_gen_utils.renderer import` — wrong import path for running tests from `python/` subdirectory | Warning | Tests fail when run from `python/` directory (error: `ModuleNotFoundError: No module named 'python'`); must be run from project root with `PYTHONPATH=.` |
| `.planning/REQUIREMENTS.md` | ~104-107, 230-232 | CODG-04/05/06/07 status not updated despite implementation complete | Warning | Creates false impression that these capabilities are missing; any status report will incorrectly show Phase 5 as not delivering these capabilities |

**No blocker anti-patterns found in generated templates or renderer code.**

---

## Human Verification Required

### 1. End-to-End Pipeline Execution

**Test:** Run `/odoo-gen:plan "equipment maintenance tracking"`, answer follow-up questions, approve the spec
**Expected:** The approval step should automatically trigger generate.md, run render-module CLI, spawn odoo-model-gen per model, then run odoo-view-gen and odoo-test-gen in parallel, then commit the generated module
**Why human:** Cannot verify interactive AI coding assistant workflow execution programmatically — requires live Claude Code session with the generate.md and spec.md workflows active

### 2. odoo-model-gen TODO Stub Replacement Quality

**Test:** Run the odoo-model-gen agent against a rendered model file containing `# TODO: implement — depends on quantity, unit_price` stubs
**Expected:** Agent replaces ALL `# TODO` stubs with correct OCA-compliant method bodies: `for rec in self:` with `rec.total = rec.quantity * rec.unit_price`; ValidationError raised (not UserError) in constraints; self.field direct assignment in onchange
**Why human:** Agent prompt-following behavior cannot be verified from file content alone; requires live agent execution

---

## Gaps Summary

**One gap found**, purely documentation/traceability:

**REQUIREMENTS.md traceability not updated:** Plan 05-01 claimed and implemented CODG-05 (`__init__.py` files), CODG-06 (data files/sequences), and CODG-07 (wizard files). The actual code demonstrably generates all of these (verified by functional render test). However, `REQUIREMENTS.md` still shows `[ ]` (unchecked) and "Pending" for CODG-04, CODG-05, CODG-06, and CODG-07. CODG-04 (action/menu XML) was not claimed by any Phase 5 plan but was also implemented. The traceability table and requirement checkboxes need to be updated to reflect reality.

This gap does not affect the system's ability to generate Odoo modules — the implementation is complete and working. It is a documentation accuracy issue.

---

## Functional Test Results

Ran a full render test with computed field + sequence field + state field + wizard spec:

```
Generated files (19 total):
  __init__.py                    (from . import models; from . import wizards)
  __manifest__.py                (canonical data ordering verified)
  data/data.xml                  (stub with Static data comment)
  data/sequences.xml             (<odoo noupdate="1"> with ir.sequence record)
  demo/demo_data.xml
  models/__init__.py
  models/test_order.py           (@api.depends stub, _default_reference, readonly/copy=False)
  security/ir.model.access.csv
  security/security.xml
  static/description/index.html
  tests/__init__.py
  tests/test_test_order.py
  views/menu.xml
  views/test_order_action.xml
  views/test_order_views.xml     (<header> with statusbar + wizard button)
  views/test_wizard_wizard_form.xml (<footer> with special="cancel")
  wizards/__init__.py
  wizards/test_wizard.py         (TransientModel + default_get + button_confirm)
```

Test suite: 122 passed, 5 warnings (0 failures)

---

*Verified: 2026-03-02*
*Verifier: Claude (gsd-verifier)*
