---
phase: 05-core-code-generation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/generate.md
  - workflows/spec.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CODG-01
  - CODG-04
  - CODG-09
  - CODG-10

must_haves:
  truths:
    - "User who approves a spec in spec.md sees a call to generate.md at the end of the approval step"
    - "generate.md orchestrates two waves: Wave 1 (render-module CLI + odoo-model-gen), Wave 2 (odoo-view-gen + odoo-test-gen)"
    - "generate.md Wave 1 completes fully before Wave 2 spawns (sequential, not concurrent)"
    - "generate.md includes a git commit step after all generation completes"
    - "REQUIREMENTS.md CODG-09 wording corrects '<list>' to '<tree>' for Odoo 17.0"
    - "REQUIREMENTS.md CODG-10 wording notes README.rst (OCA standard) not .md"
  artifacts:
    - path: "workflows/generate.md"
      provides: "Two-wave generation orchestration workflow"
      contains: "Wave 1|Wave 2|render-module|odoo-model-gen|odoo-view-gen|odoo-test-gen"
    - path: "workflows/spec.md"
      provides: "Spec workflow with generate.md trigger on approval"
      contains: "generate.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected CODG-09 and CODG-10 wording"
  key_links:
    - from: "workflows/spec.md Step 4.3 approval handler"
      to: "workflows/generate.md"
      via: "explicit call to generate.md after spec commit"
      pattern: "generate\\.md"
    - from: "workflows/generate.md Step 1"
      to: "render-module CLI"
      via: "odoo-gen-utils render-module --spec-file"
      pattern: "render-module"
    - from: "workflows/generate.md Wave 1"
      to: "odoo-model-gen agent"
      via: "Task tool spawn"
      pattern: "odoo-model-gen"
    - from: "workflows/generate.md Wave 2"
      to: "odoo-view-gen + odoo-test-gen"
      via: "Task tool spawn (parallel)"
      pattern: "odoo-view-gen|odoo-test-gen"
---

<objective>
Create the generate.md workflow that orchestrates complete module generation, add the generate.md trigger to spec.md approval, and fix two REQUIREMENTS.md documentation errors.

Purpose: This plan wires the generation pipeline together. spec.md already handles spec collection and approval; it needs one line added that triggers generate.md. generate.md is the new orchestrator that runs render-module CLI first, then agent passes. The REQUIREMENTS.md fixes correct wrong technical details that could mislead future implementors (CODG-09 says `<list>` which is wrong for Odoo 17.0; CODG-10 says .md but OCA uses .rst).

Output:
- workflows/generate.md — complete two-wave orchestration workflow
- workflows/spec.md — one-line addition calling generate.md on approval
- .planning/REQUIREMENTS.md — CODG-09 and CODG-10 wording corrections
</objective>

<execution_context>
@/home/inshal-rauf/Odoo_module_automation/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/Odoo_module_automation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/inshal-rauf/Odoo_module_automation/.planning/phases/05-core-code-generation/05-CONTEXT.md
@/home/inshal-rauf/Odoo_module_automation/.planning/phases/05-core-code-generation/05-RESEARCH.md

@/home/inshal-rauf/Odoo_module_automation/workflows/spec.md
@/home/inshal-rauf/Odoo_module_automation/.planning/REQUIREMENTS.md

<interfaces>
<!-- generate.md workflow structure from RESEARCH.md Pattern 5 (LOCKED — Decision B) -->

Wave structure (Decision B — LOCKED):
- Step 1: render-module CLI (Jinja2 structural pass)
- Wave 1 (must complete before Wave 2): odoo-model-gen fills method bodies
- Wave 2 (parallel after Wave 1): odoo-view-gen enriches views + odoo-test-gen adds computed tests
- Step 4: git commit all generated files
- Step 5: summary report

spec.md approval step (Step 4.3) currently ends with reporting commit hash and next steps.
The generate.md trigger must be added AFTER the spec commit and BEFORE the "Next steps" block.

Decision F (LOCKED): No kanban views — generate.md must NOT mention kanban generation.
Decision A (LOCKED): CRUD overrides deferred to Phase 7 — generate.md must NOT mention create/write/unlink.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflows/generate.md with two-wave orchestration</name>
  <files>workflows/generate.md</files>
  <action>
    Create a new file at workflows/generate.md. This workflow is referenced by spec.md and orchestrates the complete generation pipeline for the `/odoo-gen:plan` path (NOT the `/odoo-gen:new` quick path — that stays unchanged via scaffold.md).

    The file must contain these sections:

    **Header:**
    ```markdown
    # Generate Workflow

    End-to-end code generation from an approved spec.json.
    Called from spec.md after user approval. NOT called by scaffold.md or /odoo-gen:new.
    ```

    **Overview section:**
    Explain the two-pass hybrid approach:
    - Pass 1 (Jinja2): `odoo-gen-utils render-module` produces all structural files including # TODO method stubs
    - Wave 1 (sequential after Pass 1): odoo-model-gen rewrites model files with complete method bodies
    - Wave 2 (parallel after Wave 1): odoo-view-gen enriches views + odoo-test-gen adds computed field tests
    - Commit and report

    **Inputs section:**
    ```markdown
    ## Inputs

    - `$MODULE_NAME` — module technical name (from spec.json `module_name` field)
    - `$SPEC_PATH` — absolute path to approved spec.json (e.g., `./$MODULE_NAME/spec.json`)
    - `$OUTPUT_DIR` — output directory (default: current working directory)
    ```

    **Step 1: Render Structural Files (Jinja2 Pass)**
    ```markdown
    ## Step 1: Render Structural Files

    Run the render-module CLI to produce the complete OCA module structure:

    ```bash
    odoo-gen-utils render-module \
      --spec-file "$SPEC_PATH" \
      --output-dir "$OUTPUT_DIR"
    ```

    This produces:
    - `$MODULE_NAME/__manifest__.py` — with correct load order (sequences first, wizard views last)
    - `$MODULE_NAME/__init__.py` — imports models/ and wizards/ (if any)
    - `$MODULE_NAME/models/__init__.py` and `models/*.py` — with # TODO method stubs for computed/onchange/constrains
    - `$MODULE_NAME/views/*_views.xml` — form (with `<header>` if state field) + tree + search
    - `$MODULE_NAME/views/*_action.xml` and `views/menu.xml`
    - `$MODULE_NAME/data/sequences.xml` (if sequence fields detected) and `data/data.xml`
    - `$MODULE_NAME/wizards/__init__.py` and `wizards/*.py` (if wizards in spec)
    - `$MODULE_NAME/views/*_wizard_form.xml` (if wizards in spec)
    - `$MODULE_NAME/security/security.xml` and `security/ir.model.access.csv`
    - `$MODULE_NAME/tests/__init__.py` and `tests/test_*.py`
    - `$MODULE_NAME/README.rst`

    Verify the CLI exits with code 0 before continuing. If it fails, report the error and stop.
    ```

    **Step 2: Wave 1 — Model Method Bodies (Sequential)**
    ```markdown
    ## Step 2: Wave 1 — Model Method Bodies

    IMPORTANT: Wave 1 must complete fully before Wave 2 begins. odoo-view-gen reads the
    completed model files to add correct action button names.

    For each model in spec.models that has at least one field with `compute`, `onchange`, or
    `constrains` key:

    Spawn an odoo-model-gen Task (one per model file, can run in parallel across models):

    **Task prompt for odoo-model-gen:**
    > Read the file `$OUTPUT_DIR/$MODULE_NAME/models/{model_var}.py` and the spec at `$SPEC_PATH`.
    > Model to process: `{model_name}`.
    > Rewrite the ENTIRE model file with complete OCA-compliant method bodies replacing all
    > `# TODO: implement` stubs. Do NOT change any field declarations. Follow all REQUIRED
    > patterns from your system prompt (for rec in self:, ValidationError for constraints, etc.).
    > When done, confirm how many TODO stubs were replaced.

    Wait for ALL odoo-model-gen tasks to complete before proceeding to Wave 2.

    If a model has NO computed/onchange/constrains fields: skip it (no agent spawn needed, Jinja2 output is already complete).
    ```

    **Step 3: Wave 2 — View Enrichment + Test Generation (Parallel)**
    ```markdown
    ## Step 3: Wave 2 — View Enrichment + Test Generation

    Spawn these two agents in parallel using the Task tool:

    ### Task A: odoo-view-gen

    **Prompt:**
    > Read all view files in `$OUTPUT_DIR/$MODULE_NAME/views/` and the corresponding model files
    > in `$OUTPUT_DIR/$MODULE_NAME/models/`. Read spec at `$SPEC_PATH`.
    > For each model that has workflow_states in the spec: enrich the form view's `<header>` block
    > with action buttons for each state transition (matching action_{state} methods in the model).
    > Use `invisible="state != '{current_state}'"` pattern. Do NOT add kanban views.
    > Write enriched view files back to the same paths.

    ### Task B: odoo-test-gen

    **Prompt:**
    > Read all model files in `$OUTPUT_DIR/$MODULE_NAME/models/` and the corresponding test files
    > in `$OUTPUT_DIR/$MODULE_NAME/tests/`. Read spec at `$SPEC_PATH`.
    > For each model with computed fields, onchange handlers, or @api.constrains methods:
    > add test methods to the existing test_{model_var}.py file. Phase 5 scope: computed field
    > tests (2 per field), constraint tests (valid + invalid assertRaises), onchange tests (1 each).
    > Use TransactionCase. Do NOT duplicate existing test method names.

    Wait for BOTH tasks to complete.
    ```

    **Step 4: Commit**
    ```markdown
    ## Step 4: Commit Generated Module

    ```bash
    git add "$OUTPUT_DIR/$MODULE_NAME/"
    git commit -m "feat($MODULE_NAME): generate complete Odoo 17.0 module from spec

    Generated files: models, views, security, tests, data, wizards (if any)
    Spec: $SPEC_PATH
    "
    ```

    If git commit fails (not in a repo, no changes staged): report the issue, confirm that
    all files were written to disk, and suggest manual commit steps.
    ```

    **Step 5: Summary Report**
    ```markdown
    ## Step 5: Summary Report

    After commit, display:

    ```
    Generation Complete: $MODULE_NAME

    Files generated: {count from render-module output}
    Models: {model names}
    Views: form + tree + search per model
    Wizards: {count or "none"}
    Tests: {count of test files}

    Method stubs filled: {total TODO stubs replaced by odoo-model-gen}
    View enrichments: {count of buttons added by odoo-view-gen}

    Next steps:
    - Validate the module: /odoo-gen:validate ./$MODULE_NAME/
    - Review generated code: check models/ and views/ for correctness
    - If validation fails: /odoo-gen:validate will show specific issues to fix
    ```
    ```

    **Error handling section:**
    ```markdown
    ## Error Handling

    - **render-module CLI fails**: Stop generation. Show the error. Fix the spec.json and retry.
    - **odoo-model-gen returns error for a model**: Log the error, continue with remaining models, report at end.
    - **Wave 2 task fails**: Log the error, do not block the commit. Report which enrichment failed.
    - **git commit fails**: Report error, confirm files exist on disk, provide manual commit command.
    ```
  </action>
  <verify>
    <automated>test -f /home/inshal-rauf/Odoo_module_automation/workflows/generate.md && grep -c "Wave 1\|Wave 2\|render-module\|odoo-model-gen\|odoo-view-gen\|odoo-test-gen\|git commit" /home/inshal-rauf/Odoo_module_automation/workflows/generate.md</automated>
  </verify>
  <done>workflows/generate.md exists with all 5 steps, Wave 1 sequential guard before Wave 2, Task tool spawn prompts for each agent, git commit step, summary report</done>
</task>

<task type="auto">
  <name>Task 2: Add generate.md hook to spec.md and fix REQUIREMENTS.md errors</name>
  <files>workflows/spec.md, .planning/REQUIREMENTS.md</files>
  <action>
    **Part A: spec.md hook (one-line addition)**

    Read workflows/spec.md fully. Locate Step 4.3 "If user approves" section. It currently ends with:
    ```
    3. Report the result:
       ```
       Specification approved and committed.
       ...
       Next steps:
       - To generate the module from this spec: /odoo-gen:new {module_name}
         (Once Phase 5 is active, generation will use this spec automatically)
       ...
       ```
    ```

    After the git commit step (step 2 in the approval handler, which commits spec.json) and BEFORE the "Report the result" step, add a new step 3 that calls generate.md:

    ```markdown
    3. **Trigger code generation** — Execute the generate.md workflow:
       - Set `$MODULE_NAME` = `{module_name}` (from spec)
       - Set `$SPEC_PATH` = `./{module_name}/spec.json`
       - Set `$OUTPUT_DIR` = current working directory
       - Follow all steps in `@workflows/generate.md` to completion

       This is the automated generation pipeline. The spec commit (step 2 above) is the
       contract; generate.md reads that contract and produces the module code.
    ```

    Renumber the original step 3 (Report the result) to step 4.

    Also update the "Next steps" in the report block: remove the parenthetical "(Once Phase 5 is active, generation will use this spec automatically)" since Phase 5 is now active. Change to:
    ```
    Next steps:
    - To re-run generation: /odoo-gen:plan {module_name}
    - To validate the generated module: /odoo-gen:validate ./{module_name}/
    - To modify the spec: edit ./{module_name}/spec.json and re-run
    ```

    **Part B: REQUIREMENTS.md wording fix**

    Read .planning/REQUIREMENTS.md. Locate these two lines:

    CODG-09 (currently):
    `- [ ] **CODG-09**: All generated XML uses correct Odoo 17.0 syntax (e.g., \`<list>\` not \`<tree>\`, inline \`invisible\`/\`readonly\` expressions not \`attrs\`)`

    Fix to:
    `- [ ] **CODG-09**: All generated XML uses correct Odoo 17.0 syntax (e.g., \`<tree>\` not \`<list>\` — \`<list>\` is Odoo 18+ only, inline \`invisible\`/\`readonly\` expressions not \`attrs\`)`

    CODG-10 (currently):
    `- [ ] **CODG-10**: System generates a README.md explaining the module purpose, installation, configuration, role assignment, and usage`

    Fix to:
    `- [ ] **CODG-10**: System generates a README.rst explaining the module purpose, installation, configuration, role assignment, and usage (OCA standard is .rst, not .md)`

    These are documentation corrections only — no code changes needed (templates already use the correct patterns).
  </action>
  <verify>
    <automated>grep -c "generate\.md\|Generate Workflow\|Trigger code generation" /home/inshal-rauf/Odoo_module_automation/workflows/spec.md && grep "CODG-09" /home/inshal-rauf/Odoo_module_automation/.planning/REQUIREMENTS.md | grep -c "tree.*not.*list\|list.*18+" && grep "CODG-10" /home/inshal-rauf/Odoo_module_automation/.planning/REQUIREMENTS.md | grep -c "rst"</automated>
  </verify>
  <done>spec.md Step 4.3 approval handler includes generate.md trigger as step 3; REQUIREMENTS.md CODG-09 says tree not list (18+); REQUIREMENTS.md CODG-10 says .rst not .md</done>
</task>

</tasks>

<verification>
Confirm generate.md covers all required orchestration elements:
```bash
echo "=== Wave structure ===" && grep -n "Wave 1\|Wave 2\|Step [1-5]" /home/inshal-rauf/Odoo_module_automation/workflows/generate.md
echo "=== spec.md hook ===" && grep -n "generate.md\|Trigger code" /home/inshal-rauf/Odoo_module_automation/workflows/spec.md | head -5
echo "=== REQUIREMENTS fixes ===" && grep "CODG-09\|CODG-10" /home/inshal-rauf/Odoo_module_automation/.planning/REQUIREMENTS.md
```
</verification>

<success_criteria>
- workflows/generate.md: Exists with 5 steps, Wave 1 sequential guard documented, Task tool prompts for each agent, git commit command, summary report format
- workflows/spec.md: Contains reference to generate.md in Step 4.3 approval path
- .planning/REQUIREMENTS.md: CODG-09 says `<tree>` not `<list>` with Odoo 18+ note; CODG-10 says README.rst not README.md
- generate.md does NOT mention kanban views (deferred to Phase 7 per Decision F)
- generate.md does NOT mention CRUD overrides (deferred to Phase 7 per Decision A)
</success_criteria>

<output>
After completion, create `/home/inshal-rauf/Odoo_module_automation/.planning/phases/05-core-code-generation/05-03-SUMMARY.md`
</output>
