---
phase: 06-security-test-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - python/src/odoo_gen_utils/templates/test_model.py.j2
  - agents/odoo-test-gen.md
  - agents/odoo-security-gen.md
  - workflows/generate.md
autonomous: true
requirements:
  - SECG-02
  - SECG-04
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06

must_haves:
  truths:
    - "test_model.py.j2 generates test_write and test_unlink methods for every model"
    - "test_model.py.j2 generates test_user_can_create and test_no_group_cannot_create access rights tests"
    - "test_model.py.j2 includes AccessError import when generating access rights tests"
    - "test_model.py.j2 generates workflow transition tests only when state_field exists and workflow_states has 2+ entries"
    - "odoo-test-gen.md scope includes write/unlink/access-rights/workflow tests — Phase 5 restriction removed"
    - "odoo-security-gen.md has a complete system prompt with concrete generation instructions for standalone use"
    - "generate.md Wave 2 Task B prompt includes Phase 6 test categories"
  artifacts:
    - path: "python/src/odoo_gen_utils/templates/test_model.py.j2"
      provides: "Jinja2 test template with write/unlink/access-rights/workflow blocks"
      contains: "test_write"
    - path: "agents/odoo-test-gen.md"
      provides: "Full Phase 6 scope agent system prompt for CRUD/access rights/workflow tests"
      contains: "test_write"
    - path: "agents/odoo-security-gen.md"
      provides: "Activated security agent with complete standalone system prompt"
      contains: "standalone"
    - path: "workflows/generate.md"
      provides: "Wave 2 Task B prompt updated to Phase 6 test scope"
      contains: "Phase 6"
  key_links:
    - from: "test_model.py.j2"
      to: "state_field context key"
      via: "Jinja2 if state_field and workflow_states|length>=2 guard on workflow test block"
      pattern: "workflow_states"
    - from: "test_model.py.j2"
      to: "module_name context key"
      via: "env.ref module_name.group_module_name_user in test_user_can_create"
      pattern: "module_name"
    - from: "generate.md Wave 2 Task B"
      to: "odoo-test-gen agent"
      via: "Task tool spawn with expanded Phase 6 scope prompt"
      pattern: "Phase 6"
---

<objective>
Expand test template scope and fully activate odoo-test-gen and odoo-security-gen agents for Phase 6.

Purpose: Phase 5 left test generation at create/read/name_get (Jinja2 template) plus computed/constraint/onchange (agent). Phase 6 adds write/unlink/access-rights/workflow tests to the Jinja2 template, expands the odoo-test-gen agent system prompt to full Phase 6 scope, fully activates odoo-security-gen as a standalone expert agent, and updates the generate.md Wave 2 Task B prompt to trigger the expanded test scope.

Output:
- test_model.py.j2 expanded with 4 new test categories (write, unlink, access rights, workflow transitions)
- odoo-test-gen.md full Phase 6 system prompt replacing Phase 5-only scope
- odoo-security-gen.md complete standalone agent system prompt
- generate.md Wave 2 Task B prompt updated to include Phase 6 test categories
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-security-test-generation/06-CONTEXT.md
@.planning/phases/06-security-test-generation/06-RESEARCH.md

@python/src/odoo_gen_utils/templates/test_model.py.j2
@agents/odoo-test-gen.md
@agents/odoo-security-gen.md
@workflows/generate.md
</context>

<interfaces>
Context keys available in test_model.py.j2 (from _build_model_context() in renderer.py):

    module_name        str: e.g. "library"
    model_name         str: e.g. "library.book"
    model_description  str: e.g. "Library Book"
    model_var          str: e.g. "library_book"
    fields             list[dict]: all model fields
    required_fields    list[dict]: fields with required=True
    state_field        dict|None: the state/status Selection field (None if absent)
    workflow_states    list: from model.get("workflow_states", []) -- added to renderer.py in 06-01

NOTE: workflow_states is now a confirmed renderer.py key (added in Plan 06-01 Task 2). No renderer.py
changes are needed in this plan — 06-01 already handles all renderer.py additions.

Canonical patterns from CONTEXT.md Decision C (non-negotiable):

    write test calls .write() on self.test_record and asserts field changed.
    unlink test stores record_id, calls .unlink(), then assertFalse browse(record_id).exists().
    access rights tests: create res.users with groups_id [(6,0,[...])], use .with_user(user).
    workflow tests: only when state_field is not None AND workflow_states has 2+ entries.
    AccessError import: from odoo.exceptions import AccessError, ValidationError (top of file).

Security group ref pattern:
    self.env.ref("MODULE_NAME.group_MODULE_NAME_user")
    where MODULE_NAME is the module_name context variable.

Odoo 17.0 constraints (from knowledge base):
    - TransactionCase (not SavepointCase)
    - from odoo.exceptions import AccessError (stable since Odoo 14.0)
    - groups_id [(6, 0, [ids])] to SET group list, not (4, id) for additive
    - with_user() for ACL testing (not sudo())
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Expand test_model.py.j2 with new test categories</name>
  <files>python/src/odoo_gen_utils/templates/test_model.py.j2</files>
  <action>
    ONE file change only (renderer.py was updated in 06-01):

    CHANGE: Completely rewrite test_model.py.j2. The new file (write as a Jinja2 template):

    Line 1: {# test_model.py.j2 -- Odoo TransactionCase tests: CRUD, access rights, workflow #}
    Then write the full Python template content. The template must produce a valid Python test class that:

    1. Imports at the top:
       from odoo.tests.common import TransactionCase
       from odoo.exceptions import AccessError, ValidationError

    2. Class definition: class Test{{ model_name | to_class }}(TransactionCase):
       Docstring: Test cases for the {{ model_description }} model.

    3. setUpClass: same as existing (creates cls.test_record with required_fields values, same type dispatch table).

    4. test_create: same as existing.

    5. test_read: same as existing.

    6. test_name_get: same as existing (conditional on field.name == 'name').

    7. test_write method (NEW):
       def test_write(self):
           """Test that {{ model_description }} record values can be updated."""
       Find the first required Char field in required_fields. If found:
           self.test_record.write({"FIELD_NAME": "Updated Name"})
           self.assertEqual(self.test_record.FIELD_NAME, "Updated Name")
       If no required Char field exists:
           self.assertTrue(self.test_record.id)  # Record is still accessible

    8. test_unlink method (NEW):
       def test_unlink(self):
           """Test that {{ model_description }} record can be deleted."""
           record_id = self.test_record.id
           self.test_record.unlink()
           self.assertFalse(
               self.env["{{ model_name }}"].browse(record_id).exists(),
               "Record should not exist after unlink",
           )

    9. test_user_can_create method (NEW):
       def test_user_can_create(self):
           """Test that a user with {{ module_name }} User group can create records."""
           user = self.env["res.users"].create({
               "name": "Test Module User",
               "login": "test_user_{{ model_var }}@example.com",
               "groups_id": [(6, 0, [self.env.ref("{{ module_name }}.group_{{ module_name }}_user").id])],
           })
           record = self.env["{{ model_name }}"].with_user(user).create({
               [same type dispatch as setUpClass for required_fields]
           })
           self.assertTrue(record.id, "User with module group should be able to create records")

    10. test_no_group_cannot_create method (NEW):
        def test_no_group_cannot_create(self):
            """Test that a user without any module group cannot create records."""
            user = self.env["res.users"].create({
                "name": "No Group User",
                "login": "no_group_{{ model_var }}@example.com",
                "groups_id": [(6, 0, [self.env.ref("base.group_user").id])],
            })
            with self.assertRaises(AccessError):
                self.env["{{ model_name }}"].with_user(user).create({
                    [same type dispatch as setUpClass for required_fields]
                })

    11. Workflow transition tests (NEW, conditional):
        Guard: {% if state_field and workflow_states | length >= 2 %}
        For each consecutive pair (workflow_states[i], workflow_states[i+1]):
          Detect if each state entry is a mapping (dict with "key") or a string.
          If string: use directly as key. If dict: use entry["key"].
          Generate:
            def test_action_TO_KEY(self):
                """Test state transition from FROM_KEY to TO_KEY."""
                self.test_record.action_TO_KEY()
                self.assertEqual(self.test_record.STATE_FIELD_NAME, "TO_KEY")
        End guard: {% endif %}

    After writing, verify the template parses correctly:
    cd /home/inshal-rauf/Odoo_module_automation && python -c "
    from jinja2 import Environment, FileSystemLoader, StrictUndefined
    env = Environment(
        loader=FileSystemLoader('python/src/odoo_gen_utils/templates'),
        undefined=StrictUndefined, trim_blocks=True, lstrip_blocks=True
    )
    t = env.get_template('test_model.py.j2')
    result = t.render(
        module_name='library',
        model_name='library.book',
        model_description='Library Book',
        model_var='library_book',
        fields=[{'name': 'name', 'type': 'Char'}],
        required_fields=[{'name': 'name', 'type': 'Char', 'required': True}],
        state_field={'name': 'state', 'type': 'Selection'},
        workflow_states=['draft', 'done'],
    )
    print('JINJA2 OK')
    assert 'test_write' in result
    assert 'test_unlink' in result
    assert 'AccessError' in result
    assert 'test_user_can_create' in result
    assert 'test_no_group_cannot_create' in result
    assert 'test_action_done' in result
    print('ALL ASSERTIONS PASSED')
    "

    Also verify all existing renderer tests still pass:
    cd /home/inshal-rauf/Odoo_module_automation && python -m pytest python/tests/test_renderer.py -v 2>&1 | tail -10
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_module_automation && python -c "from jinja2 import Environment, FileSystemLoader, StrictUndefined; env = Environment(loader=FileSystemLoader('python/src/odoo_gen_utils/templates'), undefined=StrictUndefined, trim_blocks=True, lstrip_blocks=True); t = env.get_template('test_model.py.j2'); r = t.render(module_name='library', model_name='library.book', model_description='Book', model_var='library_book', fields=[{'name':'name','type':'Char'}], required_fields=[{'name':'name','type':'Char','required':True}], state_field={'name':'state','type':'Selection'}, workflow_states=['draft','done']); assert 'test_write' in r and 'test_unlink' in r and 'AccessError' in r and 'test_user_can_create' in r and 'test_action_done' in r; print('ALL OK')"</automated>
  </verify>
  <done>
    test_model.py.j2 renders correctly with all 6 test method categories (create, read, name_get, write, unlink, user_can_create, no_group_cannot_create, workflow transitions when applicable).
    All 130 existing renderer tests (122 pre-phase + 8 from 06-01) still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand odoo-test-gen.md, activate odoo-security-gen.md, update generate.md</name>
  <files>agents/odoo-test-gen.md, agents/odoo-security-gen.md, workflows/generate.md</files>
  <action>
    THREE file rewrites:

    REWRITE A — agents/odoo-test-gen.md:
    Replace the entire file. The new content:

    - Frontmatter (keep): name, description (update to remove "Phase 5 scope"), tools, color
    - description: "Generates Odoo test files. Full scope: computed, constraint, onchange, CRUD write/unlink, access rights, workflow transitions."
    - Role: Remove "In Phase 5, your scope is PARTIAL" opener. Replace with:
      "You are the odoo-test-gen agent. You generate comprehensive Odoo 17.0 test suites
      covering computed fields, constraints, onchange handlers, CRUD write/unlink, access rights,
      and workflow state transitions. You follow OCA testing standards and Odoo 17.0 patterns."
    - Input contract section: keep as-is (same inputs)
    - What you generate section: expand from Phase 5 to full Phase 6 scope:
      1. Computed field tests: same as before (2 per field)
      2. Constraint tests: same as before (valid + invalid)
      3. Onchange tests: same as before (1 per handler)
      4. CRUD write tests (NEW Phase 6): For each model, 1 test calling .write() on a field and asserting the change
      5. CRUD unlink tests (NEW Phase 6): For each model, 1 test deleting record and asserting .exists() is False
      6. Access rights tests (NEW Phase 6): 2 per model (test_user_can_create with module User group, test_no_group_cannot_create with assertRaises(AccessError))
      7. Workflow transition tests (NEW Phase 6): 1 per consecutive state pair when workflow_states exists in spec with 2+ entries
    - REQUIRED test patterns section: keep Phase 5 patterns, ADD Phase 6 patterns (write, unlink, access rights, workflow)
    - FORBIDDEN section: keep as-is
    - Execution steps: update step 4 to "Rewrite the ENTIRE test file with all test categories (not just append Phase 5 categories)"
      Add step: "5a. For access rights tests: read module_name from spec.json to build group ref"
      Add step: "5b. For workflow tests: read workflow_states from spec.json for the model"
    - Remove "Phase 6 scope note" at the bottom (it's now obsolete)
    - Knowledge base section: keep as-is

    REWRITE B — agents/odoo-security-gen.md:
    Replace the entire file. The new content must provide a COMPLETE standalone agent system prompt.
    The agent is NOT called by generate.md — it is a standalone agent for post-validation remediation.

    Frontmatter: keep name, tools, color. Update description to:
    "Standalone Odoo security specialist. Use for post-validation security fixes, custom record rules, and security audits. NOT in the generate.md pipeline (security is deterministic via Jinja2 templates)."

    Role section — complete system prompt including:
    1. Identity: "You are an expert Odoo 17.0 security engineer. You generate and fix Odoo security infrastructure (groups, ACLs, record rules) following OCA standards and the principle of least privilege."

    2. When to use this agent:
       - Post-validation remediation: user runs /odoo-gen:validate, gets ACL errors, calls you to fix
       - Custom record rules beyond multi-company (user_id ownership, team-based, date-based)
       - Security audit: review generated security files for correctness
       - Complex scenarios: portal access, field-level security, multi-level group hierarchies

    3. What this agent does NOT do:
       - Standard User/Manager group generation (handled by security_group.xml.j2 template)
       - ir.model.access.csv generation (handled by access_csv.j2 template)
       - Multi-company record rules (handled by record_rules.xml.j2 template)
       - Add itself to generate.md wave pipeline (security is deterministic, no AI needed)

    4. Execution pattern:
       Step 1: Read spec.json for the module
       Step 2: Read security/security.xml and security/ir.model.access.csv
       Step 3: Read the validation error output (from /odoo-gen:validate or user paste)
       Step 4: Identify the security gap (missing rule, wrong domain, incorrect group ref)
       Step 5: Write the fix using the correct Odoo 17.0 patterns
       Step 6: Report what was fixed and suggest re-running /odoo-gen:validate

    5. Canonical security patterns (inline, not KB reference only):
       - Module category id format: module_category_MODULE_NAME
       - Group id format: group_MODULE_NAME_user / group_MODULE_NAME_manager
       - ACL model_id:id format: model_MODULE_NAME_MODEL_NAME (dots replaced with underscores)
       - implied_ids syntax: eval="[(4, ref('group_MODULE_NAME_user'))]"
       - Record rule domain: [('company_id', 'in', company_ids)] (NOT user.company_id)
       - Global record rules: field name="global" eval="True"
       - CSV column order: id,name,model_id:id,group_id:id,perm_read,perm_write,perm_create,perm_unlink
       - User default: 1,1,1,0 (no unlink). Manager: 1,1,1,1

    6. Anti-patterns to reject:
       - Empty group_id in CSV (grants portal/public access)
       - user.company_id instead of company_ids in record rules
       - SavepointCase in tests (deprecated)
       - CSV loaded before security.xml in manifest (causes External ID not found)

    7. Knowledge base: keep the @include references

    REWRITE C — workflows/generate.md:
    Only update the Task B prompt in Step 3 (Wave 2). Find the Task B section and replace the prompt.
    The existing prompt says:
      "Phase 5 scope: computed field tests (2 per field), constraint tests (valid + invalid assertRaises), onchange tests (1 each)."
    Replace with this expanded prompt:

    "Read all model files in $OUTPUT_DIR/$MODULE_NAME/models/ and the corresponding test files
    in $OUTPUT_DIR/$MODULE_NAME/tests/. Read spec at $SPEC_PATH.
    For each model: rewrite the complete test_{model_var}.py file with all Phase 6 test categories:
    (1) Computed field tests (2 per computed field: basic + zero_case)
    (2) Constraint tests (valid + invalid with assertRaises(ValidationError))
    (3) Onchange tests (1 per onchange handler)
    (4) CRUD write test (1 per model: write a field, assert changed)
    (5) CRUD unlink test (1 per model: delete record, assert browse().exists() is False)
    (6) Access rights tests (2 per model: test_user_can_create with module User group via with_user(),
        test_no_group_cannot_create with base.group_user only, assertRaises(AccessError))
    (7) Workflow transition tests (1 per consecutive state pair when workflow_states in spec has 2+ entries,
        call action_STATE_KEY(), assert state changed)
    Use TransactionCase. Import AccessError from odoo.exceptions. Do NOT duplicate existing test method names.
    Read the module_name from spec.json to build group refs: MODULE_NAME.group_MODULE_NAME_user"

    After all three rewrites, verify:
    1. agents/odoo-test-gen.md no longer contains "Phase 5 scope note" or "Phase 5, your scope is PARTIAL"
    2. agents/odoo-security-gen.md contains "standalone" and "NOT in the generate.md pipeline"
    3. workflows/generate.md contains "Phase 6" in the Task B section

    Verification commands:
    grep -n "Phase 5, your scope is PARTIAL" /home/inshal-rauf/Odoo_module_automation/agents/odoo-test-gen.md
    (must return NO match)
    grep -n "standalone" /home/inshal-rauf/Odoo_module_automation/agents/odoo-security-gen.md
    (must return a match)
    grep -n "Phase 6" /home/inshal-rauf/Odoo_module_automation/workflows/generate.md
    (must return a match)
  </action>
  <verify>
    <automated>grep -c "test_write" /home/inshal-rauf/Odoo_module_automation/agents/odoo-test-gen.md && grep -c "standalone" /home/inshal-rauf/Odoo_module_automation/agents/odoo-security-gen.md && grep -c "Phase 6" /home/inshal-rauf/Odoo_module_automation/workflows/generate.md</automated>
  </verify>
  <done>
    odoo-test-gen.md: full Phase 6 scope (all 7 test categories documented, Phase 5 restriction removed).
    odoo-security-gen.md: complete standalone system prompt with concrete patterns and execution steps.
    generate.md: Wave 2 Task B prompt includes all Phase 6 test categories.
  </done>
</task>

</tasks>

<verification>
Run full renderer test suite to confirm no regressions:
cd /home/inshal-rauf/Odoo_module_automation && python -m pytest python/tests/ -v 2>&1 | tail -20

Verify test_model.py.j2 contains all required method names:
grep -n "test_write\|test_unlink\|test_user_can_create\|test_no_group_cannot_create\|AccessError" /home/inshal-rauf/Odoo_module_automation/python/src/odoo_gen_utils/templates/test_model.py.j2

Verify odoo-test-gen.md Phase 5 restriction is removed:
grep "Phase 5, your scope is PARTIAL" /home/inshal-rauf/Odoo_module_automation/agents/odoo-test-gen.md && echo "FAIL: restriction still present" || echo "OK: restriction removed"

Verify odoo-security-gen.md is activated as standalone:
grep -c "standalone" /home/inshal-rauf/Odoo_module_automation/agents/odoo-security-gen.md

Verify generate.md Wave 2 Task B has Phase 6 scope:
grep -A 5 "Task B" /home/inshal-rauf/Odoo_module_automation/workflows/generate.md | grep "Phase 6"
</verification>

<success_criteria>
- test_model.py.j2 generates: test_create, test_read, test_name_get, test_write, test_unlink, test_user_can_create, test_no_group_cannot_create, and conditional workflow transition tests
- test_model.py.j2 imports AccessError from odoo.exceptions at the top
- workflow_states key is consumed from renderer.py context (added in 06-01, no renderer.py changes here)
- All renderer tests still pass (130 total after 06-01 adds 8 new tests)
- odoo-test-gen.md describes full Phase 6 scope with no "Phase 5 only" restriction
- odoo-security-gen.md is a complete standalone agent with concrete OCA security patterns and execution steps
- generate.md Wave 2 Task B prompt includes all 7 Phase 6 test categories
</success_criteria>

<output>
After completion, create `.planning/phases/06-security-test-generation/06-02-SUMMARY.md` following the GSD summary template.
</output>
